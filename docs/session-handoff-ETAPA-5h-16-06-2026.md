# Session handoff — ETAPA-5h → ETAPA-5i, written 16-06-2026 (succession per D-0037, context guard ~527k)

**For ETAPA-5i (Main-Architect successor).** POINTER doc (Doctrine §9). Authoritative history = `docs/decision-log.md` **D-0046 → D-0050** (ETAPA-5h's shift) + this session's transcript. You launch on opus @ effort max, Remote-Control ON named ETAPA-5i, architect-operations canon layer. RC = the founder drives you from his phone.

**Why this handoff:** ETAPA-5h ran a long, dense shift — fixed the posting-engine integration_validator 1M-context overflow (Part B), found+fixed a finding-regeneration LOOP bug, arbitrated 3 contests, caught a real fiscal bug, drove the founder approval of the last critical stage, and integrated the whole foundation phase. **Foundation is COMPLETE (14/14 DONE), pending only the founder's phase sign-off.** This is the cleanest possible boundary (a phase frozen, the next not yet planned).

## YOUR FIRST DUTIES (in order)
1. **Write your session id** to `~/.claude/sf-architect-session` (newest `.jsonl` in `~/.claude/projects/-home-artur-projects-SF-F5/` — **verify via BIRTH time `stat -c %W "$f"`, NOT mtime**).
2. **Start your monitor** (`bash ~/.claude/sf-architect-monitor.sh`, run_in_background — harness-tracked so its exit re-invokes you). Kill any leftover by **exact cmdline** (`pgrep -af 'bash /home/artur/.claude/sf-architect-monitor.sh' | grep -v -- '--remote-control'`); update the script header to ETAPA-5i. **NOTE (D-0049):** the monitor's exit→wake had a real **latency gap** once this shift (~1h45m before re-invocation) — nothing was lost (the factory auto-heals usage-limits via the capacity_governor; the founder watches the dashboard), but don't treat the monitor as a hard guarantee. The interactive-tmux redesign targets this.
3. **Read `docs/decision-log.md` D-0046 → D-0050**, then `uv run sf-factory status`.

## STATE SNAPSHOT (16-06 ~00:38 UTC — RE-CHECK via status)
- **Factory LIVE on main @ `fe7790d`** (Part B `c97f8db` + loop-fix `a247668` + the docs). Orchestrator pid was **1284591** (re-check), watchdog ARMED. Dashboard auto-commits founder decisions to main — **FF-merge only, NEVER rebase**.
- **Foundation phase: 14/14 stages DONE, phase at `AWAITING_SIGNOFF`.** Phase-level Tier-1 + Tier-2 passed (after one infra-fail resume — see below). **Decision [8] `phase_signoff` PENDING** — the founder's final sign-off.
- **0 open escalations.** Conveyor idle, waiting on [8].

## YOUR WORK ITEMS (priority order)
1. **Foundation phase sign-off [8]** — surface to the founder if still pending (mode-1, recommend **approved**: all 14 stages DONE, the full combined suite is green on the integration branch, phase Tier-2 clean). The founder may sign it himself on the dashboard (he did that for fiscal-numbering [7]); if `status` shows it answered, skip. **NOTE:** I told the founder (00:31) I'd hand to a fresh session at this milestone and that you'd present the sign-off — so introduce yourself + present [8].
2. **C1–C4 v1 FREEZE (architect task, after sign-off)** — the foundation as-built report (`erp-workspace/.worktrees/foundation/_factory/phases/foundation/as-built-report.md`, branch `phase/foundation`) is the freeze input; it maps every C1–C4 clause to code + lists divergences/deferrals honestly. **One real architect DECISION is reserved for the freeze: the C4 E6 "15A dimension refinement"** — whether `SupplierFiscalInvoice` (15A)'s linked-document set is final or must extend to treasury's M1 service acquisitions. Read `_factory/contracts/c4-domain-events.md:13` (the E6 invariant ends with the instruction) + `docs/Business/adr/0006-posting-engine-rules.md §Service acquisition`. Surfaced in the as-built Deferrals so it does not silently drop (§12/§19).
3. **inventory-procurement planning — APPLY D-0050 (founder directive, HARD):** the next phase is **PENDING, 0 stages** — NOT yet cut. When it enters PLANNING, **do NOT accept a coarse plan**: inject a granularity directive (project context / PROJECT.md / phase-seeding guidance) OR review the produced `phase-plan.json` and `replan` if any stage is projected oversized. **Target: no stage >~100–120M tokens.** Foundation's oversized stages (auth-access 237M, status-notifications 219M, register-schemas 153M) caused the cap pressure AND the integration_validator overflow you'll read about. Standing directive for ALL domain phases.
4. **Two DOMAIN-PHASE OPEN items tracked** (do not lose — surface when the owning phase is planned): **(1)** the v1-freeze **rights-seam repoint** (D-0048): the rights backends (`STATUS_TRANSITION_RIGHTS_BACKEND`, `NOTIFICATION_RIGHT_HOLDERS_BACKEND`, `PRINT/ATTACHMENTS/NOMENCLATURE_*`) default to `superuser_only`/`superuser_recipients` — BY DESIGN (each consuming phase repoints its own backend via the settings seam), but the real `has_right` adapter must land before service-orders registers real non-superuser `zn_transition` holders. **(2)** UI-A3 (D-0049): the different-scope concurrent-reauth overlay model — for the first domain phase with two simultaneously-loading scoped screens.
5. **Lower priority:** slices 3/4 (finding-regeneration program); monitor event-update (D-0042 — exit on `escalation_opened_notice|escalation_bumped|escalation_stuck_resolved`, recognize `[arhitect]` ntfy prefix — the monitor currently MISSES a pure target-bump, as seen with [37] this shift).

## WHAT ETAPA-5h DEPLOYED/DECIDED (full detail: D-0046 → D-0050)
- **Part B (`c97f8db`, D-0047):** the merge-gate integration_validator print-prompt overflowed opus's 1M context (posting-engine: 2.36MB / ~587k conv tok + ~448k overhead). Fix: `process.tier2_max_total_bytes=1.5MB`; over budget, sibling diffs render as `@@` hunk-headers (`worktrees._hunk_headers`), gating-unit diff + contracts stay verbatim. Both Tier-2 sites (`StageExecutor._tier2_prompt` + `PhaseExecutor._tier2`) via the shared `_render_sibling_diffs`. **MEASURED, not estimated (§10 — the handoff's own lesson; the 5g "528KB≈528k-tok" was a bytes/tokens conflation; real ~4.02 B/tok).** §4 independent verify + RETRY-verified on posting-engine + status-notifications (both ran 5–7 min, not the ~2s 400).
- **Loop-fix (`a247668`, D-0048):** a `settled`/`overruled` INTEGRATION finding regenerated on the re-run merge gate (the do-not-re-raise memory `_prior_adjudications_note` was wired into `_audit_prompt` but NOT `_tier2_prompt`). Wired it into `_tier2_prompt` too. PROVEN: status-notifications re-ran, SN-INT-001 was NOT re-raised (findings `[]`), → DONE.
- **3 contests arbitrated:** [39] status-notifications `settled` (SN-INT-001 accurate-but-no-action); [40] fiscal-numbering `rework:BUILD` (caught **FN-CROSS-001**, a REAL high-severity double-storno bug — `cancel_issued_invoice` didn't `select_for_update`-reload; fixed; FN-AUD-2 dismissed §8); [43] ui-foundation `rework:BUILD` (5 real fixes incl. a token-source bypass; UI-A3 dismissed §12/§8). **Pattern (D-0049):** auditors raise accurate-but-§8/§12-premature findings alongside real comply findings; executor contests the premature one; you dismiss it in the `--reason` and `rework:BUILD` for the real ones (the contested finding is `sustained` — empirically benign, no loop; if it ever loops, `settled` it as a sole-finding round).
- **integration-seed [7]** founder-approved (he clicked it on the dashboard); foundation integrated after **[44] `integration_conflict`** which was actually an **infra-fail** (`pg.sh: port 5433 occupied` — a just-finished stage's test cluster hadn't released it; `rebase_conflict=false`, failed at postgres init) → resolved `resume`, passed on the free port.

## WORKING-MODE LEARNINGS (keep)
- **Redeploy ritual** (used 2× cleanly this shift; full steps in `docs/runbooks/first-live-run.md` + D-0047): disarm watchdog (`sudo -n systemctl disable --now sf-factory-watchdog.timer`) → confirm 0 expensive agents → baseline `wc -l .factory/run-live.log` → `tmux send-keys -t factory C-c` → poll `kill -0 <pid>` dead (~2s) → `fuser .factory/factory.db`=none → fresh `tmux new-session -d -s factory ... '.venv/bin/sf-factory run 2>&1 | tee -a .factory/run-live.log'` → wait `entering scheduler loop` past baseline → re-arm watchdog → relaunch monitor → resolve. **D-0032 order: the code/config must be LIVE before you resolve the escalation that depends on it.**
- **Clean window** = 0 EXPENSIVE factory agents (`comm -23 <(pgrep -f 'output-format stream-json|codex exec'|sort) <(pgrep -f -- '--remote-control'|sort) | wc -l`). Deploy only then; frozen/escalated stages keep a stable window.
- **`pgrep -af` on factory agents DUMPS THE FULL CANON into your context** (their cmdline is the canon) — use the `comm`/narrow form above, never `-a` on them.
- **Measure, don't estimate** (§10): reconstruct real inputs (e.g. `WorktreeManager` + the real method against the live worktree) before sizing a fix. Burned a retry on Part A by trusting an estimate; Part B/loop-fix were measured + §4-verified.
- **Founder:** Romanian, glossed (no bare IDs/escalation numbers), brutal honesty (§21), cost-conscious, drives by phone, **answers gates directly on the dashboard**, front-loads decisions. He directed smaller stages (D-0050) and watches closely.

## YOUR SUCCESSION
Finish your work unit, write the handoff, launch ETAPA-5j, **VERIFY 5j's RC on the founder's phone BEFORE going silent** (predecessor RC is the fallback), hand the marker, go silent. Don't keep working after the successor takes the marker (sole-writer rule).
