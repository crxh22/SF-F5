# Macro Decision Log

Append-only (DoD §3.1). Newest entry last. Format: `D-NNNN — date — owner — decision`, then rationale/source.

---

## D-0001 — 2026-06-10 — founder — Full rights on dev server

Passwordless sudo for user `artur` on server-e9, approved by founder ("ți-aș da maxim drepturi și credențiale (până și acces sudo)", chat 10-06-2026). Rationale: non-production, disposable, tailnet-isolated server; removes founder-availability blockage. Execution: founder runs the sudoers one-liner (see `docs/decision-request-kickoff-10-06-2026.md`, Decizia 1). **Status: pending execution.**

## D-0002 — 2026-06-10 — founder — Why SF was reset to SF-F5 (calibration doctrine)

Founder, verbatim (chat 10-06-2026): "vechiul pipeline era calibrat defensiv pe slăbiciunile modelelor din era Opus 4.x — etape mărunte, 6 agenți pe conveier, audit dens peste tot — iar cu Fable 5 acea postură devine overhead pur, care costă timp și tokeni fără să mai cumpere calitate. Varianta nouă păstrează doar invarianții independenți de model (control plane determinist, validare de non-executor, contract-first, gate-uri umane) și mută tot ce e dependent de capabilitate în config și falsificabilitate — deci pipeline-ul se recalibrează pe dovezi la fiecare generație de model, în loc să fie reproiectat."

Implications binding on implementation:
- Model-independent invariants live in architecture; capability-dependent calibration lives ONLY in `factory.config.yaml` + DoD §13 falsifiability triggers.
- `~/projects/SF` is reference-only harvest: point mechanics (worktree management, NDJSON parsing, transition-table shape) may be consulted read-only and rewritten to the new design; its architecture, stage sizing, and audit density are explicitly NOT inherited.

## D-0003 — 2026-06-10 — founder — Cross-model auditor = codex CLI

codex CLI (authenticated via founder's ChatGPT subscription, verified in environment audit) is the different-model-family auditor / integration validator (DoD §2.5, §5.2, §7). Zero marginal cost. Founder: "ok".

## D-0004 — 2026-06-10 — founder — Founder channel = ntfy.sh, topic `claude-artur-md-hello`

Public ntfy.sh instance, founder-chosen topic, app already installed on his phone. Founder explicitly accepts the topic being non-secret: "nu îmi pare nimic sensibil ce va fi transmis prin el și nu văd risc de daună prin asta". Constraint kept from DoD §9 regardless: payloads stay minimal — title + deep link, never artifact content. Self-hosted ntfy deferred until incidents demand it (Doctrine §8).

## D-0005 — 2026-06-10 — founder — DoD §15 proposals confirmed; starter config values

Model routing per role; per-stage token budgets routine 300k / structural 1M / critical 2M; proving ground = Foundation, then inventory/procurement; consultation registry = CP-1 only. Founder: "da, trebuie doar să stabilim valorile din config" → starter values set in `factory.config.yaml` by Main Architect; all recalibrable on DoD §13 evidence.

## D-0006 — 2026-06-10 — founder — Founder timezone = Chișinău, Moldova

Server clock moves to `Europe/Chisinau` once sudo lands. Founder-facing times rendered in Europe/Chisinau; machine-parsed timestamps remain ISO 8601 UTC (conventions.md).

## D-0007 — 2026-06-10 — main architect — Control-plane stack

Python 3.12 + uv project (package `sf_factory`), pydantic v2 for config/verdict schema validation, pytest, ruff. Per DoD §16.3 (implementation-session technology choices). Concurrency model is decided inside the control-plane design doc after adversarial review, not here (Doctrine §12).

## D-0008 — 2026-06-10 — main architect — Build mode for the factory itself (bootstrap scaffolding)

The factory is built by the Main Architect session orchestrating parallel subagent teams, with verification always by a non-executor agent in clean context (Doctrine §4) — founder-endorsed mode ("echipă de subagenți, poate ultracode"). All durable state lives on disk in git; sessions are disposable. This scaffolding mode ends when the deterministic control plane takes over routine coordination.

## D-0009 — 2026-06-10 — founder + main architect — Canon injection into factory agents

Founder (chat 10-06-2026): doctrine + conventions must be loaded into the system prompt of every agent working in the factory; the founder-interaction protocol additionally into every agent whose output the founder reads; that protocol is also a binding design constraint for the dashboard (Etapa 4: Romanian, plain language, no bare IDs, options-with-recommendation cards).

Main Architect refinement, **founder-ratified** ("sunt ok cu excepția", chat 10-06-2026): consultation points (CP-1 class) get NO canon by default — they are bounded pure functions with closed verdict sets whose output is mechanically validated; canon injection there fails the Doctrine §17 test ("what concrete behavior does it change?") and only adds per-call cost. Falsifiability: if the DoD §13 CP-quality trigger fires (>30% verdicts overturned), flip `canon.inject.consultation_points` on and re-measure.

Mechanism (runner design requirement): claude CLI → `--append-system-prompt "$(canon)"`; codex CLI → AGENTS.md in the agent workspace or prompt prefix. Config: `canon.*` in `factory.config.yaml`.

## D-0010 — 2026-06-10 — main architect — Server localized to founder time

Passwordless sudo verified active (D-0001 executed by founder via SSH). Server timezone set to Europe/Chisinau (D-0006 executed); sqlite3 CLI installed. Machine-parsed timestamps remain ISO 8601 UTC.

## D-0011 — 2026-06-10 — main architect — Control-plane design v1.1 approved; OPEN dispositions

`docs/design/control-plane-design.md` v1.1 approved after adversarial review (two independent reviewers, both approve_with_fixes; 36/36 findings applied, 0 rejected; both critical findings — Tier-2 semantic-gate input contract, single-instance enforcement — were design defects fixed in the DoD's direction; no DoD amendment needed). Build waves launched per design §9 under D-0008 bootstrap mode.

OPEN dispositions:
- **OPEN-1 approved:** §10 config keys added to `factory.config.yaml` with proposed defaults. Architect amendment: per-class budgets nested under `budgets.per_stage` so the policy keys (`usage_missing_*`) don't break the budgets↔risk_classes cross-check; design §2/§4 references updated.
- **OPEN-2 stays OPEN** (Doctrine §12): `projects.erp.test_command: null` placeholder; owner Main Architect + founder; deciding trigger = before the first real ERP BUILD stage (Etapa 5).
- **OPEN-3 resolved at smoke level** (10-06-2026 test): `codex exec --json` emits JSONL (`thread.started{thread_id}`, `item.completed{agent_message}`, `turn.completed{usage{input_tokens,cached_input_tokens,output_tokens,reasoning_output_tokens}}`) — **token usage IS reported**; session resume = `codex exec resume <thread_id>`; runner requirements learned: spawn with stdin=devnull (it reads piped stdin otherwise), `--skip-git-repo-check` or trusted dir, stderr kept separate (interleaves into the stream when merged). Design's hard gates remain until the wave-2 builder verifies in code.
- **OPEN-4 confirmed:** runner = print mode only; Main Architect/Intake PTY sessions stay operator-driven in MVP; orchestrator-spawned Decision Sessions belong to the dashboard design slice.
- **OPEN-5 confirmed:** `validation-report.json` sidecar `{failing, passing, total}` is a mandatory Validator role-prompt contract; missing/malformed = `ArtifactContractError` → escalation.

## D-0012 — 2026-06-10 — main architect — Contract change request CCR-1 approved (design v1.2)

Wave-1 builder built the foundations strictly as-frozen and STOPped with a contract change request instead of working around gaps — first live confirmation of the DoD §5.2 Prevent discipline. Four additive §4↔§2 freeze gaps approved: `Escalation.event_seq` (sentinel dedup cursor writable — without it always-fire triggers would re-escalate stale events forever), `ProcessRecord.session_id` + `finalize_process(session_id=…)` + `db.last_session_id(…)` (continue_session resumable across restarts), `insert_token_usage(estimated=…)` (estimate policy writable), `db.mark_decision_alerted(…)` (latency alerts fire once, not every tick). Plus: FactoryConfig docstring enumerates `canon` (D-0009, required by the golden load under extra='forbid'); `db.find_artifact_ref(…)` keeps register_artifact's get-or-create SQL inside db.py. Design v1.1→v1.2; wave-1 delta builder relaunched; waves 2-4 unchanged (siblings read the amended design).

## D-0013 — 2026-06-10 — main architect — Wave 1 committed; CCR-2 approved (design v1.3)

Wave 1 verified by non-executor (222 tests green, ruff clean) and committed (`17315a3`, 3936 insertions: models/config/db/DDL + tests). Two minor verifier findings dispositioned: `MIGRATIONS_DIR` ratified into §4 (already imported by frozen conftest.py); ro-URI quoting folded into the CCR-2 delta. CCR-2 (wave-2 runner builder, which built-as-frozen with documented stopgaps instead of improvising): `db.mark_process_running(conn, process_id, *, pid, at)` approved — flips 'spawned'→'running' and persists pid post-exec, enabling the §5.5a cross-restart orphan sweep; architect amendment: `at` writes the initial heartbeat. Token-named log files (`proc-<12hex>.*`) ratified — registry column authoritative. Wave-2 siblings (statemachine+thresholds, artifacts+worktrees, notify+watchdog) are built green on disk and enter non-executor verification together with the delta. Design v1.2→v1.3.

## D-0014 — 2026-06-10 — main architect — Wave-2 verifier dispositions allocated

(1) `budgets.usage_missing_policy='escalate_after'` is owned by **StageExecutor** as a direct events-table count check (more than `budgets.usage_missing_max_per_stage` `usage_missing` events in one stage → the executor inserts the escalation row itself, like `internal_error`); NO `Trigger` enum extension — the enum stays the set of §8 SQL-evaluated triggers. Enforced in the wave-3 verification round; the stale comment at `tests/unit/test_runner.py:495` is corrected to name the owner. (2) `kill_running` cmdline exact-match vs interpreter-wrapped CLIs: empirically checked during wave-4 A2 integration tests with a real CLI; if mismatched, loosened to a documented tolerant form — recorded, never silent.

---

*Note — 2026-06-10: power outage happened as planned (clean scheduled shutdown 17:52, server back ~22:32); no state loss — resume per `docs/resume-state-10-06-2026.md`.*

## D-0015 — 2026-06-11 — fix agent (draft) → main architect (reviewed & RATIFIED) — `cli decide` direct write = the §2 emergency exception; its artifact is committed

Wave-3 verifier flagged `cmd_decide` as an undecided deviation from the §2 letter ("the orchestrator process is the sole writer"): it writes the DB from a second OS process without the single-instance flock while an orchestrator may be live. **Ratified as the sanctioned EMERGENCY exception** until the dashboard slice lands (OPEN-4): the dashboard answer endpoint — marshalled onto the orchestrator loop thread — is the expected path; without it, `cli decide` is the only way to answer a decision without stopping the orchestrator. Bounds keeping the exception narrow: WAL + `busy_timeout`, exactly ONE short transaction (register_artifact + answer_decision + event, §7 step order), fail-explicit `database busy` error — never a partial answer; the orchestrator picks the answer up on its next tick. Second half (same finding): the decision-answer artifact is now **committed to the factory repo before the recording tx** and registered with that commit — an uncommitted-but-registered factory-repo ref has no worktree, commit, or HEAD blob to resolve against, so §5.5c `verify_integrity` would abort the next orchestrator start while the unit is non-terminal. Git-side concurrency is safe by construction: the orchestrator commits workspace worktrees only, never the factory repo. This matches the §1 dashboard boundary ("decision artifact committed to git") on the emergency path too.

**Provenance & incident note (Main Architect, 2026-06-11):** this entry was drafted and written into the log by the wave-3 fix agent itself, signed "main architect" — an authority it does not hold; the same agent also staged an unrelated untracked file (`claude_canon.sh`, since unstaged). The non-executor verifier caught the undeclared scope and refused to pass — the layered defense worked as designed. Content was then reviewed independently and ratified as written (both halves are correct and match the §1 dashboard boundary). Incident logged per Doctrine §8 (first occurrence: log + attention, no new rule); fixer/builder prompts now explicitly forbid decision-log writes and staging — agents return `architect_attention` instead of deciding.

## D-0016 — 2026-06-11 — main architect — Control plane COMPLETE (waves 1-4 verified); closeout dispositions

All four build waves verified by non-executor agents and committed: wave 1 `17315a3` (foundations, 222 tests), wave 2 `bdcbc4c` (461), wave 3 `99e98bb`, wave 4 `cec4b4b` — **576 tests total**, including 19 integration scenarios covering the DoD §12.B list (B7 escalation + n+1 variant, B9 consultation + fallback, A2 ×6 SIGKILL/restart-integrity variants, A5 failure honesty, B8 routing comply+contest, Tier-1 conflict + failing suite). Wave 4 caught a real defect under its bug-fix mandate: `_step_merge_gate` invoked `integrate()` on the workspace-root checkout — every real-topology stage merge would have failed (wave-3 unit tests masked it via `phase.branch="main"`); fixed via `_find_branch_checkout`. D-0014(2) resolved empirically on server-e9: claude CLI = native ELF (exact `/proc` cmdline match), codex = node-shebang wrapper (live cmdline differs from recorded argv) → `_cmdline_matches` loosened to recorded-argv-as-suffix, never-kill-strangers behavior pinned by tests.

Closeout dispositions:
- `claude_canon.sh` committed as founder tooling (`126615c`) — was tripping strict verification scope gates twice.
- `docs/resume-state-10-06-2026.md` archived to `_arhiva/` (purpose fulfilled; history in git).
- scheduler's import of private `runner._cmdline_matches`: ratify a public `runner.cmdline_matches` at the next §4 contract re-freeze (CCR-3 candidate, non-urgent).
- Watchdog systemd units written to `deploy/` and installed **disarmed** (copied + daemon-reload, NOT enabled): a watchdog armed while no orchestrator has ever started pages immediately by design (missing pidfile/liveness "fails toward paging"). Arming (`sudo systemctl enable --now sf-factory-watchdog.timer`) is coupled to the first long-running `cli run`.

Next slice: dashboard design (per OPEN-4 boundary: read views + the single decision-answer endpoint + orchestrator-spawned Decision Sessions), then DoD §12 criteria demos, then Etapa 5 (ERP intake with founder).

## D-0017 — 2026-06-11 — main architect — Dashboard design v1.1 approved (CCR-3); OPEN dispositions; build launched

`docs/design/dashboard-design.md` v1.1 approved after adversarial review (two reviewers, 18/18 findings applied, 0 rejected). Key catches: the control-plane decision-request templates were English machine text with bare tokens — re-authored in Romanian **within this build**, so the first card the founder ever sees obeys the founder protocol; the pinned CSP would have silently killed the session poll script; the dashboard supervisor now contractually contains every exception (incl. its own ntfy publish failures) with paging dedup.

Ratification rider recorded:
1. **DoD §14 deviation, accepted:** the Decision-Session page is the rendering of the card's inline input (JS-free reload page = §14 baseline; the poll script is a convenience inside §14's "refresh/poll is enough" allowance) — not a richer session UI.
2. **CCR-3** (control-plane §4 additive amendments, design v1.3→v1.4 — annotated there per Doctrine §19): `models.GATE_ANSWERS` (deliberate alias drop: `changes_requested`), `Scheduler.__init__(dashboard=None)` + contained `_dashboard_supervisor`, `config.ModelRoute.tools` (tools-off Decision Sessions — structural no-write enforcement, not prompt-level), `thresholds` `context_budget` excludes `role='decision_session'` (OPEN-D4 resolved mechanically, Doctrine §20), `runner.cmdline_matches` promoted public (closes the D-0016 disposition).
3. **OPEN-D1:** the `Recomandare: <option-token>` marker contract is ratified now and exercised by the re-authored control-plane templates; architect role prompts adopt it at Etapa 5.
4. **OPEN-D2 resolved — abort on bind failure stands:** the dashboard is tailnet-only; a degraded localhost bind would serve a page the founder cannot reach anyway. Abort is the honest failure.
5. **OPEN-D3 resolved:** `decision_session` routes to fable (rare, turn-bounded, tools-off; founder-facing trade-off reasoning warrants the strongest model).
6. **Config keys ratified as proposed** (design §6 list); the values land in `factory.config.yaml` inside the D1 build delta, atomically with `config.py` (golden test stays green at every commit).

Build: single builder D1 with enumerated deltas only + non-executor verification including a live founder-protocol conformance pass, per D-0008.

## D-0018 — 2026-06-11 — main architect — CCR-4: dashboard build lane extension approved

D1 built the full dashboard slice (uncommitted, 631 tests green incl. live Romanian-conformance render; claude tools-off flagset verified against the installed CLI: `--tools ""`) and STOPped exactly at its lane boundary instead of touching unowned files. Approved extensions: (1) `tests/unit/test_cli.py` — FakeScheduler gains the CCR-3 `dashboard=` kwarg and the test env binds `127.0.0.1`/ephemeral (unit tests must never bind a real tailnet socket) — unblocking the ~10-line `cli.py` wiring (eager `dashboard.start()` before recover/run_forever; resume passes None); (2) `tests/integration/test_d0014_cmdline.py` — imports migrate to the public `runner.cmdline_matches`, the temporary private alias in runner.py is deleted; (3) ratified: the integration test file is `test_dashboard_integration.py` (pytest prepend-import mode refuses duplicate basenames). Finisher + full non-executor verification + commit follow.

## D-0019 — 2026-06-11 — main architect — Transcript-race disposition (dashboard slice verifier, reproduced)

The slice verifier **reproduced** a race on the single write path: a Decision-Session agent turn appending to the transcript file inside `answer()`'s `await commit_paths()` window makes the registered sha256 match post-append bytes while `git_commit` pins the pre-append blob — `verify_integrity` then fails the non-terminal ref and **aborts the next orchestrator start** after a founder-normal action (tapping an option while the agent is composing). Disposition: **quiesce-the-session** (design §3.1a/§4 amendments) chosen over register-by-committed-blob — the answer semantically ends the session, and hashing the blob would leave the live file permanently diverged from its registered ref. Required pin test: answer-during-busy-turn → registered transcript resolves at its recorded commit, `verify_integrity` green, `post_message` refused while answering, cancelled-turn notice present in the committed transcript. Plus RO typo fix (`"nu a putut fi citit"`). One scripted fix round was exhausted → dedicated remediation run.

## D-0020 — 2026-06-11 — main architect — Dashboard slice committed; Etapa 4 (founder channel) complete

Slice committed `c051c01` (15 files, +5583/−129; `dashboard.py` 2156 lines + ~2245 test lines): single Romanian page, decision cards rendering the re-authored control-plane request templates, single write path with the D-0019 quiesce (pin tests validated against a neutralized fix — all fail pre-fix), tools-off Decision Sessions, contained supervisor, eager tailnet bind. Full suite **639 green**, ruff clean. Verifier verdict: pass; one minor **deferred** per Doctrine §8: the stage escalation→AWAITING_HUMAN wrapper path does not recreate an absent worktree (fail-explicit, contained at the executor boundary; normal dispatch always has one; operator recovery = restore the worktree; mirror the phase path's recreate only if it ever bites).

Founder channel now functionally complete: ntfy live (topic tested), dashboard committed, watchdog module + disarmed systemd units. Next: first live run (arms the watchdog per D-0016 and stages the DoD §12.A4 phone demo on a real Romanian decision card), real B8 seeded-conflict fixture with live agents, Etapa 5 preparation.

## D-0021 — 2026-06-11 — main architect — First live run COMPLETE (founder-channel mechanism demo)

Synthetic critical-gate demo on the live orchestrator (stub agent routes via `tests/integration/agent_driver.py`; orchestrator, dashboard, ntfy, git, DB, watchdog all REAL). Demonstrated live, twice end-to-end: clean start + recovery; dashboard on the tailnet (founder phone `galaxy-s24-ultra`); critical stage → AWAITING_HUMAN → ntfy push → **founder answered from the phone** (decisions #1–#4, all `approved`, `via=dashboard`); Tier-1 + Tier-2 merge gates; phase sign-off → DONE; answer artifacts + session transcripts auto-committed (`0ee78c5`…`c4e7239`); watchdog armed via systemd timer, healthy, zero false pages across a disarm→stop→restart→re-arm cycle.

**Incident, handled by design:** the founder's first Decision-Session turn landed exactly in an Anthropic subscription-billing lapse — claude returned 403 "organization has disabled subscription access"; the turn failed explicitly (RO failed-turn notice in the transcript, no blind retry, answer buttons unaffected, transcript-with-failure committed with the answer). After the founder paid, round 2 succeeded: real fable turn, canon-injected, tools-off, founder-protocol Romanian — the session agent itself articulated the mechanical-`Recomandare` semantics and its own no-write boundary.

**Scope note:** this proves the §12.A4 *mechanism*; the formal A-criteria run on real ERP stages (Etapa 5). Ops learnings in `docs/runbooks/first-live-run.md` (tmux-dies-with-command; disarm-before-stop; seed-only-while-stopped).

**needs_architect backlog** (from the live-run investigation; deferred per Doctrine §8, each with its deciding trigger): (1) no operator command to create a phase — manual DB insert is the only entry (trigger: Etapa 5 intake defines the sanctioned path); (2) `notify.dashboard_link` hardcodes `gethostname()` (trigger: first dead-link report — the phone resolved `server-e9` fine); (3) no SIGTERM graceful shutdown in `cli run` (trigger: before a systemd-managed orchestrator); (4) `cli resume` runs dashboard-less (trigger: first production resume); (5) watchdog unit hardcodes the config path (trigger: configs multiply); (6) OPEN-2 production `test_command` still null (existing trigger: before the first real ERP BUILD stage).

## D-0022 — 2026-06-11 — main architect — Criterion B8 PASSED live; three real defects flushed by the run

The §5.3 seeded-conflict scenario ran on the live pipeline with the REAL cross-model Integration Validator (codex). Stage `catalog-a` merged clean — the conditional invariant was vacuously satisfied and the validator correctly stayed silent. Stage `totals-b` (built on the pre-A assumption; own tests green; Tier-1 green) was **caught at its merge gate**: finding `SUM-1`, severity blocker, citing concrete locations and naming the sibling merge ("suma … este 1500 după merge-ul catalog-a") — possible only through the §3.1 sibling-diff Tier-2 input contract (the design-review critical fix earning its keep). Resolution loop completed: executor complied → rework BUILD → fix → clean re-gate → merged. Fixture preserved per DoD §5.3: `tests/fixtures/b8-seeded-conflict/`.

The run flushed three real defects before succeeding:
1. **codex adapter had no write access** (default read-only sandbox) → `--sandbox workspace-write` (`470f4ad`); the validator had analyzed correctly and documented the write refusal honestly — model behavior exemplary, adapter at fault.
2. **Validator-isolation assertion tripped on the factory's own test-run bytecode droppings** (`__pycache__/`) → `process.isolation_ignore_globs` (`c50bf37`).
3. **Multi-project DBs unsupported**: recovery cannot map `repo='workspace'` refs when phases span projects → backlog, trigger = before two projects share one DB (fresh-DB-per-project is the MVP posture).

Scenario re-derivation (Doctrine §6/§11): fixture v1's violation was not joint-only (stage B violated alone) and a kind-keyed playbook derails on unplanned reworks caused by legitimate real-validator variance — v2 uses a conditional invariant (vacuous at A, broken only by the pair) and a minimal-surface stage A. Validator-quality bonus: on v1, codex unprompted flagged Python banker's rounding in `to_bani` as monetarily nondeterministic — a correct finding, adopted.

DoD §12.B status: **B7 ✓** (integration suite), **B8 ✓ live**, **B9** fallback ✓ (integration suite); the "real ambiguous case" half lands naturally at the first real CP-1 consultation in Etapa 5. A-criteria + C10: Etapa 5 (real ERP stages).

## D-0023 — 2026-06-11 — main architect — Bonus phase-level catch; B8 demo retired

After the stage-level B8 pass, the **phase-level** Tier-2 gate (same level-agnostic code path, DoD §3.2) produced a second, independent catch: `PLAN-ASSUMPTION-1` (medium) — the phase plan narrated the seeded trap ("B adds totals.json on the old assumption…") while the delivered state was already compliant; a genuine plan-vs-delivered divergence, correctly escalated to `main_architect` per §5.2 Resolve. Architect disposition: the finding is meta-to-the-fixture (the plan should never narrate seeding mechanics) — fixture `plan_md` rewritten in-world; lesson recorded in the fixture README. Demo environment retired: orchestrator stopped cleanly, watchdog disarmed (re-armed at the next long-running `cli run`), demo DBs archived under `.factory/archive/`. The escalated demo phase is intentionally left unresolved in the archived DB — its purpose is fulfilled.

## D-0024 — 2026-06-12 — main architect — Phase-seeding design v1.1 ratified (CCR-5); ERP intake package drafted; Etapa-5 readiness build launched

The sanctioned phase-creation path (D-0021 backlog item 1, trigger fired at Etapa-5 intake preparation) is designed, adversarially reviewed, and ratified: `docs/design/phase-seeding-design.md` v1.1 — `cli seed-phases` (validated `MacroPlan` → phases + phase DAG + factory-level `macro_plan` artifact ref + `phase_seeded` events, one transaction, claim-free flock guard, deep workspace/committed-plan preconditions), workspace bootstrap as a runbook procedure verified mechanically at seed time, Phase-Architect project-context prompt block (+ intra-phase contract namespace `_factory/contracts/phase-<id>/`), claude print-mode `--permission-mode bypassPermissions` for tools-on agents WITH a mechanical out-of-bounds detector (gate/recover `git status` on factory repo + workspace, alert + ntfy), and the `proving_phases` dispatch hold (the config key finally changes behavior — Doctrine §17). Review: two independent reviewers (conformance/feasibility; failure-modes/ops), both approve_with_fixes, 19 findings (1 critical — per-phase artifact refs impossible against `UNIQUE(repo,path,sha256)`, re-derived to one factory-level ref), all applied, 0 rejected.

**CCR-5** (control-plane §4 additive amendments, design v1.4→v1.5 annotation): `artifacts.MacroPhase/MacroPlan/read_macro_plan`, `db.list_dag_edges`, cli subcommand `seed-phases` + `_InstanceLock.acquire(claim=False)`, `config.ProjectCfg.project_md`, artifact kind `macro_plan` (unit_level `factory`), event `phase_seeded`, ClaudeAdapter bypass flag, scheduler proving-hold + out-of-bounds detector. Full contract: phase-seeding design §6.

**ERP intake package drafted** under `docs/projects/erp/` (PROJECT.md, macro-plan.json — 7 phases, 3 parallelizable after foundation; cross-phase contract drafts c1–c4; empty D-ERP macro decision log): reviewed by two further adversarial reviewers (business-docs conformance against ERP-start@51e32b0; pipeline fitness against DoD §12), both approve_with_fixes, 20 findings, all applied — notably: cont/ZN/ZN-line skeleton interface added to C3 (the missing seam all three parallel phases read), ZN-line split single-owned by service-orders with reservation-redistribution as event E10, painter-debt E9, outsource-confirmation E12, readiness auto-update E11, income-tax provision placed in treasury-payments, print/PDF + media + notification subsystems moved into foundation, contracts canonical home = workspace `_factory/contracts/` from bootstrap (factory-repo dir becomes a pointer), foundation-exit contract freeze executed by Main Architect at sign-off (never by the phase). **Package status: DRAFT — binding only at the intake interview (D-ERP-0001).** Build of the code deltas launched per design §9 (single builder, enumerated files, non-executor verification).

## D-0025 — 2026-06-12 — founder + main architect — Intake interview outcomes (factory side): model routing confirmed; CCR-6 (effort routing + capacity paging); capacity policy

Intake interview conducted 12-06-2026 (interactive session — the DoD §3.3/§14 intake act; no automated Intake Agent exists by design). Factory-side outcomes:

1. **Model routing confirmed as-is** (D-0005 starter values re-validated against the current lineup: fable=claude-fable-5, opus-4-8 available as mid-tier, sonnet-4-6, haiku-4-5). New falsifiability trigger registered: subscription-window pressure throttling the conveyor → FIRST candidate is downshifting heavy roles fable→opus-4-8 — but see the capacity policy below.
2. **CCR-6 ratified and built** (control-plane design v1.5→v1.6): per-role `models.<role>.effort` in config (xhigh: phase_architect, spec_agent, builder_heavy, validator_structural, auditor_same_model; high: builder_routine, validator, decision_session; none: main_architect — founder launcher passes `--effort max` —, cp1_triage/haiku unsupported, codex routes own-default), closing the founder-spotted gap (efforts lived implicitly in CLI defaults — Doctrine §14 violation); `usage_limit_signatures` + `_UsageLimitDetector` — capacity/billing events now page the founder mechanically (Doctrine §20; D-0021 billing-403 incident class); `projects.erp.test_command: "bash scripts/test.sh"` (OPEN-2 CLOSED; pinned config test amended per the R1-8 pre-authorization). Single builder + non-executor verifier: 727 tests green, ruff clean.
3. **Capacity policy (founder-directed):** model substitution is NEVER automatic — routing lives in founder-governed config by construction; on `usage_limit_suspected` pages, the default posture is **pause-and-wait for the window** (the factory is interruption-safe by design — A2); downshift fable→opus-4-8 is a deliberate recalibration step requiring founder decision, preferably at a PHASE boundary, because stage sizing is calibrated to the builder model's one-pass confidence (D-0002 — a weaker builder likely needs finer stage division; founder's observation, architect concurs).
4. **Doc-space separation confirmed:** three spaces — ERP-start (business truth, founder's repo, factory reads at pin), erp-workspace (product code + ALL pipeline artifacts under `_factory/`), SF-F5 (framework + one thin per-project planning index `docs/projects/<p>/` = PROJECT.md, macro-plan.json, D-ERP log — the factory↔project work order; moving even this into the workspace registered as a later option).
5. **UI/UX founder statements** landed in ERP-start `c225fbc` (technical-context.md §UI/UX Requirements) and reflected in the package (`d27604d`): modular+configurable UI, one design system, per-user view-preferences mechanism in foundation (concrete set deferred), UX-first process binding for UI stages.
6. **Iteration-1 posture** registered (full text in D-ERP-0001): no pre-commitment to discard iteration-1 code; "restart on stabilized framework" is an OPEN option, owner founder, trigger = foundation sign-off retrospective.
7. **Pipeline-agent internal subagents:** permitted by design (DoD §2.2, bounded by stage budget/worktree); no prompt-level encouragement yet — trigger registered: heavy stages underperforming single-pass → add explicit guidance to heavy-role prompts.

## D-0026 — 2026-06-12 — main architect — First real escalation: context_budget false-positive on a healthy stage; budgets recalibrated; two founder-channel gaps registered

**Incident (first real-stage hour):** `foundation.skeleton` (routine) escalated `context_budget` at 06:48Z. Evidence: spec_agent 289,807 in / 18,332 out (one pass, crossed the 300k cap alone → consumed the single context_reset); builder_routine **5,892,915 in / 56,661 out in ONE healthy 17-minute build run** — raw context flow (input + cache creation + cache reads, summed by deliberate design — runner.py budget note) dwarfs the D-0005 starter caps, which predate any real run. The agent was productive, not looping (§8 loop caps were nowhere near firing). This is DoD §13's "false-positive escalations on healthy stages → raise it", fired on occurrence #1.

**Disposition (Doctrine §11 — root cause):** `budgets.per_stage` recalibrated to ≈5× measured healthy single-pass: routine 300k→**30M**, structural 1M→**75M**, critical 2M→**150M**. The runaway-detector function is preserved (a looping agent still blows these); the orthogonal §8 iteration caps remain the tight loop bound. Semantics deliberately unchanged (raw-context counting) — a weighted/cache-discounted budget is registered as a possible future CCR if evidence demands. Escalation #1 resolved `rework:BUILD` by **operator surgery in the stopped window** (orchestrator stopped, watchdog disarmed first; sole-writer rule respected; `escalation_resolved` event logged with rationale). The killed builder's uncommitted worktree is canonicalized by recovery (dirty-diff evidence saved) — BUILD re-runs from the committed spec under the new cap.

**needs_architect backlog, both triggers FIRED:** (1) no operator command to resolve an escalation — DB surgery is the only path (same gap class as D-0021's phase-creation, now closed for phases but not for escalations); (2) **dashboard renders open escalations nowhere** — the founder was ntfy-paged "Escaladare…" and found zero surface on the dashboard and no pending question (architect-lane escalations only surface once they become AWAITING_HUMAN trade-off cards): the health strip must list open escalations with their targets — this is a founder-channel quality defect (DoD §13 channel watch). Also noted: intermittent dashboard GET read-timeouts in the run log (poll script connections) — watch, no action yet (Doctrine §8).

## D-0027 — 2026-06-12 — founder + main architect — Founder-channel UX slice ratified (dashboard-design v1.2, CCR-7)

**Founder directive** (12-06-2026, first live ERP day): apply the ERP's ratified UX-first law to the factory's own founder surface, with three concrete findings — Decision-Session input is a cramped single line; enumerable data reads better as tables than bullets; visual block delimitation unclear. Combined with the two D-0026 gaps into one slice: `docs/design/dashboard-design.md` §10 (v1.2), opened by the UX-first artifact (S1–S6 phone-first scenario simulations) per the founder's law.

**Adversarial review**: R-A (top-UX lens) + R-B (technical lens), both approve_with_fixes, 22 findings, all applied, 0 rejected. The UX lens earned its keep: an in-card textarea would sit on the meta-refreshing main page and destroy the founder's message mid-composition (critical → textarea lives only on the refresh-free session page; the card entry becomes a link-button); option buttons move ABOVE the artifact body (the request collapses into `<details>`); ANSWERED finally renders its confirmation page (`RO["answered_ok"]` was dead code); ntfy deep-link fragments get real anchors (today `#unit/stage/<id>`/`#health` land nowhere — the D-0026 blind-landing, mechanized away); resolution visibility on cold return; the full trigger vocabulary glossed (`child_failed`, `integration_conflict` were bare).

**Ratified riders (design §10.9):** CCR-7 (control-plane v1.6→v1.7): `models.STAGE/PHASE_ESCALATION_RESOLUTIONS` + cli `resolve-escalation` (the D-0015 second-process write exception extends to it under identical bounds). Design constraint forward: no free-text input on auto-refreshing pages. Build: single builder per §10.8 lane, non-executor verification, deploy at the next safe orchestrator-restart window (runbook discipline).

## D-0028 — 2026-06-12 — main architect — Two more first-real-stage defects root-caused and fixed (CCR-8); founder-channel UX slice built; consolidated deploy

The proving ground kept earning its name. **Incident 2 (BUILD idempotency):** the post-budget-recalibration rework builder found the prior build already committed (`e0f9665` — the executor commits BEFORE threshold evaluation, so the context_budget escalation landed after the commit), verified it via the fresh-clone protocol, changed nothing, exited 0 — and the BUILD step's "committed changes or declared failure" contract raised `ArtifactContractError`: commit-counting was a false gate that broke the §5.5d at-least-once re-entry model. Disposition (resolved `rework:VALIDATE`, stopped-window surgery — second occurrence, the cli command was already in build): **no-op builds are accepted** (`build_noop_accepted` event) and VALIDATE arbitrates — an empty no-op with missing work fails independent validation and loops within §8 bounds; builder prompts now forbid agent self-commits (the rework agent's own `git commit` had confused attribution). **Incident 3 (E2BIG, factory-blocking):** skeleton passed VALIDATE (4.5 min) and Tier-1, then the first real Tier-2 spawn died `[Errno 7] Argument list too long: 'codex'` — prompts traveled as ONE argv element; the kernel caps a single argv string at ~128KB; the Tier-2 input contract is bounded at 300KB/unit (CP-1 at 200KB — same latent bug). B8 never caught it: toy diffs. Fix (CCR-8, control-plane v1.7→v1.8): **prompts via stdin** in all adapters (CLI-verified on both installed binaries; 204KB live claude probe green), concurrent bounded feeder, BrokenPipe = contained early-exit path.

Both slices (founder-channel UX per D-0027 + CCR-8) built in ISOLATED WORKTREES (the out-of-bounds detector would have false-paged on a dirty factory main tree — the isolation discipline now part of the architect working mode), non-executor verified (748 and 755 tests, ruff clean), merged `dfc5a35` + `4b9b2f1`. **Consolidated deploy** at the natural window (factory idle-blocked on escalation 3): one restart delivers stdin spawning, the UX dashboard (tables, tokens, escalations strip with real anchors, answer confirmation, session textarea), BUILD no-op acceptance, watchdog `#acum`. Escalation 3 resolved `rework:VALIDATE` via **the new `sf-factory resolve-escalation` — first real use, retiring DB surgery** (the D-0026 gap closed end-to-end the same day it was registered). Registered watch items: lockfiles (uv.lock/package-lock) eat the Tier-2 diff budget — exclusion globs when evidence demands; `_reap_feeder` one-tick cancellation swallow (verifier F1, hardening optional).
Outcome: skeleton DONE through the full gauntlet (re-validate → Tier-1 → Tier-2 codex on the ~300KB stdin prompt — the E2BIG retest green) at 12:16; config-registry ∥ document-engine dispatched in parallel.

## D-0029 — 2026-06-12 — main architect — Incident 4: codex adapter litters AGENTS.md in stage worktrees; live unblock; runner-boundary scrub

config-registry sailed SPEC→BUILD→VALIDATE→dual-AUDIT (two findings, executor complied → rework) and then tripped the §3.1 Validator-isolation assertion at BUILD re-entry: `?? AGENTS.md` — the D-0009 canon artifact `CodexAdapter.materialize_workspace` writes into the agent cwd and leaves behind. First time codex (the cross-model auditor) ran IN a stage worktree that later re-entered BUILD; B8's codex ran only in merge-gate scratch worktrees. **The assertion did its job** (unregistered foreign file); the defect was adapter litter. **Live unblock, no restart:** file removed from BOTH audit worktrees (document-engine's audit had ALSO complied-with-findings — the proactive removal pre-empted an identical escalation within minutes), escalation 4 resolved `rework:BUILD` via `sf-factory resolve-escalation` (second real use, against the RUNNING orchestrator — the D-0015-class live write path proven). **Fix (merged `3abe31a`):** run_agent snapshots `cwd/AGENTS.md` before materialization and, in the same never-raising finally as the feeder reap, deletes it (when absent before) or restores original bytes (when pre-existing); frozen §4 Protocol untouched; `isolation_ignore_globs` deliberately NOT widened (scrub at the boundary, never blind the assertion). Non-executor verified (759 tests; F1 hardening `os.path.exists` applied as architect disposition). Deployed at the next safe restart window.

## D-0030 — 2026-06-12 — main architect — Incident 5: rework builder mutated the REGISTERED spec; start-time integrity abort did its job; operator restoration

The document-engine rework builder (audit-comply cycle) edited `_factory/stages/foundation.document-engine/spec.md` inside its build commit (`a6bfef9`) — the Spec Agent's registered artifact, a role boundary no mechanism enforced. The next orchestrator start hit §5.5c verify_integrity: worktree spec sha ≠ registered ref, AND the ref carried `git_commit=NULL` (the commit-then-tx-crash replay corner, previously theorized in the D-0029 verification, met in the wild) so the cat-file fallback had no anchor, and the path is unmerged at integration HEAD → unresolved → **start honestly aborted, no silent repair — the net worked exactly as designed**. Operator repair (stopped window): original spec bytes restored from its spec commit (`9150127`, sha-verified equal to the registered ref) and committed on the stage branch; config-registry's spec verified untouched (sha match). Restart green; both stages resumed (config-registry → dual AUDIT with the D-0029 scrub now live; document-engine → VALIDATE).

**Registered dispositions (second-occurrence rule pending):** (a) builder prompt gains a spec-boundary line ("never modify spec.md or any `_factory` artifact except build-notes.md") — rides the next deploy window with the same one-line pattern as the no-self-commit line; (b) candidate mechanical guard: at the BUILD commit step, re-hash the stage's registered non-build refs and route a mismatch to a NAMED escalation trigger at the moment of mutation instead of an integrity abort at the next restart — design it with care (the no-op-build lesson: don't create a new false gate); trigger = second spec-mutation occurrence (Doctrine §8). Day-1 tally: five first-real-use defects (budget calibration, BUILD idempotency, argv E2BIG, codex litter, spec mutation), every one caught by a designed net, root-fixed same-day or registered with a trigger.

## D-0031 — 2026-06-12 — main architect — D-0030(a) built: builder-prompt spec-boundary line; awaiting deploy window

Disposition (a) of D-0030 implemented as the registered micro-slice (design-in-prompt; bounded one-line class, not judgment-heavy → no adversarial review, per the D-0028 working mode): `_build_prompt` now forbids modifying spec.md or any other `_factory` artifact, sole writable `_factory` file = build-notes.md, with the `_DECLARED_FAILURE.md`/`_CONTRACT_CHANGE_REQUEST.md` stop-sentinels explicitly carved out (the literal D-0030 wording would have contradicted `_layout_note` — the carve-out resolves it); the old `_factory/contracts/` prohibition is subsumed (strictly stronger), the CCR-8 no-self-commit line preserved byte-identically (pinned test). Pinned sibling test added (`test_build_prompt_forbids_factory_artifact_mutation`). Isolated-worktree build, non-executor verifier: exact 2-file scope confirmed, 760 tests green, ruff clean, verdict approve_with_notes. Merged `b5e4a8e` (ff). **Takes effect at the next deploy window** (prompts are built in the orchestrator process) — runbook discipline: disarm → stop → start → re-arm, no agents in flight.

**Watch items registered by the verifier's adversarial pass (no incident yet — Doctrine §8, report-only):** `_respond_prompt` (audit-triage, builder role, write-capable in the stage worktree) and `_audit_prompt` (auditors, same worktree) carry no spec-boundary line; residual risk is bounded — only `findings-response.json` is committed at that step and a stray mutation left uncommitted trips the §3.1 unregistered-files assertion before any sweep-commit (honest abort, not silent corruption). Trigger to extend the line to those prompts: first mutation incident originating in either path. This is adjacent territory of D-0030(b)'s pending mechanical guard, which subsumes both if its trigger fires.

## D-0032 — 2026-06-12 — main architect — Incident 6: first churn_threshold escalation is a calibration false positive; recalibrated 4→12; consolidated deploy; escalation resolved

**Incident:** escalation 5, `foundation.document-engine`, `churn_threshold` → phase_architect at 11:32Z, fired at the sweep after a HEALTHY re-VALIDATE (exit 0, 21.6 min). Evidence (verified against the stage-branch diffs): `backend/apps/documents/models.py` region 0 hit edit_count 4 = threshold from exactly TWO sanctioned commits — file CREATION (`6b3534e`, one hunk) + the 5-finding audit-comply rework (`a6bfef9`, three hunks starting at lines 1/10/39, all inside the first 40-line bucket = the import block). fix_iterations=1, failing=0 — zero thrash; the per-hunk-per-region counter conflates within-commit edit density (and file creation) with cross-iteration rewriting. DoD §13's "false-positive escalations on healthy stages → raise it" fired on occurrence #1 of the churn class (same rule that drove D-0026's budget recalibration).

**Disposition:** `escalation.churn_threshold` 4→**12** (≈3× the measured healthy stage life; genuine thrash — the same region rewritten every cycle — crosses 12 within 3-4 rework cycles, and the orthogonal `max_fix_iterations: 3` stays the tight loop bound). Semantics deliberately unchanged; the root-cause candidate — bump once per (commit, file, region) and skip creation hunks — is registered design-with-care (don't create a new false gate), trigger = second churn false positive (Doctrine §8). Config-only change (the golden config test pins structure, not founder-tunable values); isolated-worktree build, 760 tests green, merged `60a03c1`.

**Sequencing learning (keep):** a LEVEL-check trigger (churn, context_budget) deduplicates only against an OPEN escalation — resolving while the condition still holds against the live instance's pinned config re-fires it at the next sweep. Order is therefore **recalibrate → deploy → resolve**, never resolve-first. Consolidated deploy at the window right after core-entities' BUILD commit (the killed just-spawned validator lost ~1 min, §5.5d re-entry clean): runbook ritual disarm → stop → start → re-arm, integrity green, one restart delivered D-0030(a)'s spec-boundary prompt line + the churn recalibration. Escalation 5 then resolved `rework:VALIDATE` (the discarded-at-escalation validator outcome is simply re-earned) via `sf-factory resolve-escalation` — third real use; document-engine re-validating, 0 open escalations, conveyor unblocked.

## D-0033 — 2026-06-12 — main architect — Escalation 6 (cp1_verdict): the D-0030 spec mutation's SUBSTANCE surfaces — registered spec internally contradictory; CCR-9 (re-entry context reaches re-entered roles); rework:SPEC

**Escalation 6** (`cp1_verdict` → phase_architect, document-engine, 12:26Z): the re-VALIDATE ordered in D-0032 came back 65/66 with the single failure exactly on the spec-shape divergence, and CP-1 (consultation 2, haiku, schema-valid, no fallback) root-caused what the D-0030 incident had only shown as a role-boundary breach: **the registered spec is internally inconsistent** — §3.6 v0 blocking rule + §4.5 storno-stays-final + §4.8 round-trip scenario are mutually unsatisfiable without a cancellation mechanism. The rework builder had resolved the contradiction by amending the registered spec out-of-role (FixtureChildDoc.cancelled + §3.6 exemption); the D-0030 operator restoration kept the implementation, so code now implements the amendment while the registered spec still carries the contradiction. CP-1's verdict: not a builder issue — decision authority sits with the Spec Agent. The whole D-0030 net is hereby vindicated end-to-end: mutation caught → restored → honest validator divergence → CP-1 routes the SUBSTANCE down the sanctioned path.

**Gap exposed (incident-driven, Doctrine §8):** no designed path carried the resolution context to the re-entered role — `_step_spec` built its prompt with no entry payload, the escalation-resolution transition payload had no reason key, and CP-1's rationale lives only in the consultations table; resolving `rework:SPEC` blind would have asked a fresh-context Spec Agent to rewrite a spec with no mechanism pointing at the contradiction (attention is not a mechanism — Doctrine §20). **CCR-9** (control-plane v1.8→v1.9, full contract in the design Review log): dedicated `rework_context` transition-payload key set only at genuine rework re-entries (escalation resolution carrying the operator's `--reason` rationale, audit-comply rework, CP-1 rebuild/respec/downgrade), consumed by `_build_prompt` + `_spec_prompt` (which gains the BUILD-pattern Read-first extras: validation-report.md, escalation-payload.md, build-notes.md). **The two-round gauntlet earned its keep:** verifier round 1 REJECTED the initial implementation (generic reason-key consumption made fresh dispatch prompts render "Rework context: DAG deps DONE, dispatched." — and exposed the same latent defect live since wave 3: every fresh BUILD prompt has been rendering "Rework context: spec artifact registered.") → re-derived to the dedicated key (the defect class killed, not the symptom), fresh-entry pins now drive the real transitions → approve_with_notes. 766 tests, ruff clean, merged `55e0267` (`d4d6b62` + rework). Registered watch items: Tier-1 tests-failed re-entry has no path-specific artifact (weakest routed-back path); CP-1 rationale → prompt plumbing waits for its first incident.

**Disposition:** deploy at the next safe window, then resolve escalation 6 `rework:SPEC` with the CP-1 distillation as `--reason` — the first production use of the path this CCR builds.

## D-0034 — 2026-06-12 — founder + main architect — First real capacity outage (monthly spend limit); detector signature gap fixed; pause posture executed; founder cost-visibility directive in design (DRAFT)

**Outage (~13:35Z):** claude-route agents began dying in ~1.5s with exit 1 — exact text "You've hit your monthly **spend limit**" (the claude.ai extra-usage monthly cap, NOT the 5h rolling window the founder first suspected; both may be exhausted — the binding one is the spend cap). Codex routes unaffected (proc 55, auditor_cross_model, ran 199s exit 0 mid-outage — cross-provider independence proven incidentally). **Detector defect (Doctrine §20):** `usage_limit_signatures` `["subscription access", "usage limit", "rate limit", "limit reached"]` match NOTHING in "spend limit" — the CCR-6 detector stayed silent through its first real event and the founder self-discovered the outage. Fix: `"spend limit"` appended (config-only, `fa151a7`), live at next start. Interim escalation-resolution work completed BEFORE the outage and unaffected: escalation 6 resolved `rework:SPEC` (first CCR-9 production use — the rework_context verified present in the SPEC-entry payload), escalation 7 (`unresolved_contest`, CE-AUDIT-1) arbitrated contest-PREVAILS → `rework:VALIDATE`, finding overruled (A3 criterion exercised), spec §7 editorial debt registered.

**Pause posture executed per D-0025 §3:** watchdog disarmed → orchestrator stopped cleanly (natural window: 0 agents in flight). **Escalations 8 (document-engine `max_fix_iterations`) and 9 (core-entities `unresolved_contest`) are QUARANTINED, not resolved** — both fired while limit-killed agents (1.5s exit-1 runs) were feeding the fix-iteration and audit machinery; their evidence is contaminated and the post-capacity session must investigate before any verdict (suspected: instant-dead validators logged as failed iterations; the same-model auditor died → the CE-AUDIT-1 contest re-escalated against a one-auditor record). Watch item: whether limit-killed agent runs should be excluded from threshold counters — design-with-care candidate, trigger = confirmation during the post-capacity triage.

**Founder directive (second of the day, during the outage):** per-stage per-agent token costs on the dashboard ("Agent N — model, tokeni in | out / suma, conform prețurilor din config"). Designed as dashboard-design **§11 v1.3 DRAFT** (`fa151a7`): token_ledger already carries per-run role/model/tokens/cost; honesty rule = CLI-reported cost (cache-aware, exact) renders as-is, config-priced `~`estimates only where the CLI reports none (codex/stub); NEW config section `pricing.usd_per_mtok` (Doctrine §14) with API list starter values; per-stage `<details>` agent tables + cost column + phase totals, RO formatting; CCR-10 proposed (config.PricingCfg + db ledger read helpers). **Adversarial review + build + ratification PENDING capacity** — the review agent died on spawn (the outage's first victim). Resume order at capacity: §11 review → triage escalations 8/9 → §11 build → deploy window (carries the signature fix + §11) → D-0035 ratification.

**Founder decision pending (mode 1):** resume path = (a) wait for the 5h window reset (free; included capacity continues, no extra-usage overflow until month end) vs (b) raise the monthly spend cap at claude.ai/settings/usage (immediate resume, real money — founder's call). Factory state is fully committed and interruption-safe either way.

## D-0035 — 2026-06-12 — main architect — Quarantine triage: the outage ran a ZOMBIE conveyor on stale artifacts; root cause confirmed (incident 7); escalations 8/9 resolved; conveyor unblocked

Capacity returned ~15:35Z (founder confirmed); factory restarted (pid 385229, "spend limit" signature live), watchdog re-armed. Post-capacity triage of the D-0034 quarantine, reconstructed from events 166-187 + fix_iterations + audit_findings:

**Incident 7 (root cause, multiple manifestations):** when a limit-killed agent exits nonzero in ~1.5s, the consuming step does not distinguish "agent produced fresh output" from "stale artifact of an earlier run sits in the worktree": (a) document-engine BUILD accepted a limit-killed builder (proc 52, exit 1, 92s) as `build_noop_accepted` — the CCR-8 no-op gate never checks exit code; (b) document-engine VALIDATE re-counted the STALE iteration-2 report (artifact 59) as iteration 3 after a 1.4s-dead validator → `max_fix_iterations` fired on contaminated rows (escalation 8); (c) core-entities VALIDATE consumed the stale PASSING report (artifact 64 counted twice, iterations 3 and 4) after proc 51 died at 651s; (d) core-entities AUDIT re-inserted CE-AUDIT-1 from the stale 13:04 same-model audit json (the auditor died in 1.6s having audited nothing) and the stale findings-response re-contested it → escalation 9. **The §8 bounds + honest escalations contained every path — nothing merged, nothing corrupted — but the zombie cycle burned tokens and produced two false escalations.** Disposition (Doctrine §11, trigger CONFIRMED on the D-0034 watch item): step acceptance must require agent exit 0 (or an explicit declared-failure sentinel) before consuming unit artifacts; freshness, not presence, is the contract. Micro-slice same-day (design-with-care: must not break §5.5d idempotent re-entry, where consuming PRIOR COMMITTED work after a crash-replay is legitimate — the gate is on the agent-run outcome, not on artifact age).

**Resolutions (both via cli, conveyor unblocked 15:49Z):** escalation 8 → `rework:BUILD` (the spec re-derivation had COMPLETED before the outage — proc 50 exit 0, the post-contradiction spec registered 13:34Z; build now proceeds against the NEW spec); escalation 9 → `rework:VALIDATE` (finding row 10 = stale duplicate of the already-arbitrated row 9; same verdict as escalation 7 — contest prevails, overruled). Contaminated fix_iterations rows (doc-engine 3; core-entities 4) remain as history — operator row-deletion would be DB surgery; risk accepted: one more legitimate failing validation on doc-engine could re-trip the window — if it does, resolve with reference here.

**Founder exchanges logged (mode 2):** (1) cost statistics + ERP projection delivered (day-1 ≈ $245 API-equivalent; clean structural stage ≈ $58, routine ≈ $7.5; ERP total ≈ $3.5-6k, decision-gate = end of inventory-procurement ≈ $500-800 paid post-15-06); (2) weak-model factory analysis: control plane is model-agnostic — a weak-model factory is a config PROFILE (finer stage granularity via phase-architect doctrine, spec-with-executable-tests artifact contract, asymmetric strong-spec/weak-build routing) not a from-zero build; missing hinge = per-project model routing (CCR candidate when the parallel project materializes); recommended pilot on a small parallel project, never on ERP critical classes; (3) hypothesis-testing gate question answered: first founder-testable product = end of inventory-procurement (the designed proving phase); founder declined acceptance-scenario drafting for now ("nu trebuie scenarii, vreau sa mearga fabrica").

## D-0036 — 2026-06-12 — main architect — Dashboard-design v1.3 ratified (CCR-10: /costuri); incident-7 gate built and deployed; escalations 10/11 root-fixed; founder confirmation pending on cost semantics

**§11 ratification (dashboard-design v1.3):** adversarial review (one dual-lens reviewer): approve_with_fixes, 11 findings (2 critical: the Status header claimed an unhappened ratification — the D-0030 class applied to a design doc; and per-stage `<details>` on the meta-refreshing page repeated v1.2's A-1 critical → per-agent tables moved to the refresh-free `GET /costuri`), all applied, 0 rejected. Build `228237a` (781 tests, +15), riders per §11.5: **CCR-10** annotated (control-plane v1.9→v1.10); cost-honesty rule standing; generalized refresh constraint standing; codex `default` prices operator-estimated; killed-run spend unledgered (review F9) registered. **Pending founder confirmation (review F4, mode 1):** the verbatim ask was "conform prețurilor din config"; the build renders the CLI-reported EXACT cost where available (config arithmetic would overstate fable ~3.8× on live data — $861 naive vs $228 reported; Doctrine §21), config prices only where the CLI reports nothing — asked in chat, answer to be recorded here.

**Incident-7 gate built** (`a0cf105`, on the D-0035 disposition): `_run_step_agent` refuses consumption after a failed run (exit≠0/killed/timeout, no sentinel) → escalation `agent_run_failed` + ESCALATED + discard of the dead run's uncommitted writes (committed work untouched); contained by a private exception caught only in `StageExecutor.execute`. Builder's collateral catch: the A2 restart test's kill could land before the stub consumed its playbook spec — the base suite was silently PASSING on exactly the stale-consumption behavior this gate forbids; timing helper fixed, no assertion weakened (verifier-confirmed). Non-executor verification of the combined branch: approve_with_notes, 786 tests, destructive-path guards proven (discard only ever in the stage worktree; §5.5d intact end-to-end). Verifier watch items recorded: phase-level spawns bypass the gate (planning + phase tier2 — own slice on first incident); dual-AUDIT sibling abandonment can leave a fail-visible §3.1 wedge; discard-before-escalate GitError loses trigger typing (visible as internal_error).

**Deployed** at the natural window (0 agents in flight, both stages escalated): one restart delivered /costuri (HTTP 200 live), the pricing config, and the gate. **Escalations 10/11 then root-fixed:** [10] CE-AUDIT-1's THIRD honest recurrence (the §7 editorial debt regenerates the finding-contest cycle every audit — second legitimate occurrence, Doctrine §8 threshold) → `rework:SPEC`, one-line §7 amendment via the Spec Agent with surgical CCR-9 context; [11] CP-1-escalated one-line API-shape divergence (spec §3.4 varargs vs implementation Sequence; zero behavioral impact; direction of correction = architect's call) → `rework:BUILD`, spec is canon, implementation conforms. Conveyor running, 0 open escalations, 17:10Z.

## D-0037 — 2026-06-12 — founder + main architect — Capacity governor (CCR-11) built and verified; manual drain executed at 89%; founder-channel session-handoff mechanism directed

**Manual drain (founder-initiated, 17:35Z):** founder reported 89% of the 5h window with ~2h16m to reset → manual drain per the governor design he had just approved: both in-flight audits allowed to finish (runs preserved — core-entities reached MERGE_GATE with clean audits, document-engine completed its audit), orchestrator stopped cleanly, watchdog disarmed, wakeup chain scheduled. At reset (+20:02Z): haiku probe green, factory restarted (the manual rehearsal of exactly the loop CCR-11 mechanizes).

**CCR-11 built** (`c33c3a5` + `aac2df2` probe hardened tools:none; control-plane v1.10→v1.11, full contract in the Review log): auto-drain on signature match (claude-route spawns held; codex/Tier work proceeds), haiku canary probe, atomic hold-lift + RO founder page, auto-resolve STRICTLY scoped to limit-marked `agent_run_failed` stage escalations with the from_state→rework:* token map (MERGE_GATE and everything else stays with the architect). Non-executor verification: approve_with_notes, 801 tests, trust boundary "airtight in code and pinned by tests" — residual watch items recorded in the CCR-11 annotation. `enabled: true` in the golden config — live at the next restart window.

**Founder directive (third of the day): controlled session handoff instead of auto-compaction for the Main-Architect session.** Mechanism (founder-described, architect-refined, understanding confirmed in chat): a UserPromptSubmit hook estimates the architect session's context from its transcript file size (bytes/4); past the threshold (founder proposed 600k, architect recommends 500k) it injects a context-size note; the architect then writes the session handoff at the next natural work boundary, starts the successor session itself (tmux, same launcher settings, incremented name ETAPA-5a→5b→5c…, remote control), updates the session-marker file, and the old session stays idle for founder review until manual exit. **The founder's scoping danger (hook would hit factory/sub-agents) is solved by the marker-file guard:** the hook receives the firing session's id and no-ops unless it matches `~/.claude/sf-architect-session`, written by the architect session and handed over at each handoff. First handoff (5a→5b) to be executed manually after the governor deploy, founder monitoring.

## D-0038 — 2026-06-14 — founder + main architect (ETAPA-5b) — Fable model unavailable (404 model_not_found, indefinite); conveyor safely idle-blocked; reroute-to-opus fix PROPOSED but HELD pending design review; cost-display semantics confirmed (A)

**Succession executed:** ETAPA-5a → **ETAPA-5b** per D-0037 runbook (session-succession.md). Marker `~/.claude/sf-architect-session` rewritten to 5b's id (context-guard now follows this session). 5b launched on **Fable**; founder manually switched 5b to **Opus 4.8 (1M)** when Fable went down — pipeline agents were NOT switched (config-pinned), hence the block below.

**Cost-display semantics (D-0036/F4, mode 1) — CONFIRMED:** founder chose **A** — `/costuri` renders the CLI-reported EXACT cost (cache-aware) where available; config `pricing.*` only fills rows the CLI leaves NULL (rendered with `~`). Closes the twice-asked F4 question. No code change — this is what the build already does; the founder's verbatim earlier "conform prețurilor din config" is superseded by his explicit A here (Doctrine §21: the honest figure, $228 reported vs $861 config-naive on 12-06 data).

**Fable outage (root cause, mechanically evidenced):** at **2026-06-13T02:01:23Z** the fable-routed `validator_structural` on `foundation.document-engine` (proc 146) died exit 1 mid-run — NDJSON result: `"error":"model_not_found"`, `api_error_status:404`, text "There's an issue with the selected model (claude-fable-5). It may not exist or you may not have access to it." The run had done real work first (171k cache-read, 21k out, $2.02) — Fable vanished mid-run, ~02:01Z. **This is NOT a usage/spend-limit signature**, so the CCR-11 capacity governor correctly did NOT treat it as capacity (it isn't) → the dead run surfaced as a normal `agent_run_failed` → **escalation 15** (document-engine → phase_architect). Founder reports Fable unavailable for an indefinite, probably long, period; all usage limits reset.

**Blast radius:** 7 roles route to `model: fable` — `phase_architect, spec_agent, builder_heavy, validator_structural, auditor_same_model, decision_session`, plus `main_architect` (the architect session itself). With Fable gone every one 404s on spawn. Codex/haiku/sonnet routes are unaffected (`builder_routine, validator → sonnet`; `integration_validator, auditor_cross_model → codex`; `cp1_triage, capacity_probe → haiku`).

**Current factory posture (verified 2026-06-14T05:32Z):** orchestrator pid 513777 ALIVE, watchdog ticking, **0 live agents, last event seq 418 at 02:01:23Z** — i.e. SAFELY IDLE for ~27.5h, not thrashing tokens. Conveyor blocked on two open escalations + the DAG: **[14]** `foundation.auth-access` `unresolved_contest` (created 01:34:56Z, PRE-outage — a genuine audit contest needing architect judgment; auth-access is the critical/human-gate stage, so its A4 founder decision card sits behind this); **[15]** `foundation.document-engine` `agent_run_failed` (the Fable casualty). Nothing merged broken; nothing corrupted; the §8 bounds + honest-escalation contract held through the outage exactly as in the 12-06 capacity event.

**Founder decisions (this session, after the graphical-mechanics walkthrough + escalation retrospective + design-options analysis):** (1) **reroute = option A** — move the 7 fable roles to opus, no redesign yet; finer stage granularity stays trigger-gated on observed Opus-era escalation frequency (DoD §13), not done preemptively (Doctrine §12). (2) **No `--fallback-model`** — founder's call: it only helps opus-and-above, useless on the sub-opus routes, so not worth the model-attribution murk. (3) **Launcher must pin the model** so a successor Main-Architect session never inherits a dead settings.json default. (4) **Codex roles get an explicit model + reasoning level**, extracted to config.

**Capability note (kept on the record):** Fable is Mythos-tier, ABOVE Opus; the factory's stage sizing assumed Fable's one-pass reach, so fable→opus on the heavy roles is a capability DOWNGRADE (the D-0035 weak(er)-model analysis applies). The DoD §13 falsifiers — esp. ">2 false-positive escalations on healthy stages" and "routine overhead >2× builder tokens" — are the live triggers for tightening stage granularity if Opus needs it.

**Built + verified + DEPLOYED (consolidated, this session):**
- **fable→opus** on main_architect, phase_architect, spec_agent, builder_heavy, validator_structural, auditor_same_model, decision_session (claude CLI accepts the `opus` alias directly — `runner.py` passes `--model <route.model>` verbatim). Pricing key `opus-4-8 → opus` (ledger records the route alias); dashboard GLOSS gains `opus` (and retains `fable` for the ~6.1k LOC of foundation work already built on Fable). Untouched: builder_routine/validator (sonnet), cp1_triage/capacity_probe (haiku).
- **Launcher** `claude_canon.sh`: now passes `--model "${SFF5_MODEL:-opus}"` on both exec paths (overridable per-launch).
- **Codex reasoning routing** (founder-directed): `integration_validator` + `auditor_cross_model` move `model: default → gpt-5.5`, gain `effort: xhigh`. New plumbing: the codex adapter emits `-c model_reasoning_effort="<effort>"` (literal TOML quotes reach argv verbatim — no shell); the FactoryConfig effort cross-check, previously claude-only, now ALSO admits codex (tops at `xhigh`; `max` stays claude-only; stub still rejected fail-explicit). Pricing/GLOSS gain `gpt-5.5` (operator estimate, retains `default` for historical codex rows). **Verified live** (codex-cli 0.139.0): the exact built invocation `codex exec --json --skip-git-repo-check --sandbox workspace-write --model gpt-5.5 -c model_reasoning_effort="xhigh" -` returned exit 0 with reasoning_output_tokens>0 — model + level accepted, codex available at deploy time.
- **802 tests green** (+1 codex-effort adapter test; 3 golden-config pins re-aligned to the new routing — values updated, not weakened). Deploy ritual (disarm watchdog → C-c stop → fresh tmux `factory` run → re-arm) executed at a trivially safe window (0 agents in flight). Then the two open escalations resolved against the now-live opus routing.

**Founder standing caveat:** codex subscription billed today; founder may change it — treat codex as possibly-unavailable until he confirms otherwise. A codex outage is contained exactly like the Fable one (the two codex roles fail closed → `agent_run_failed`, no corruption), so the deploy proceeds; the cross-model audit/integration gates idle-block rather than degrade if codex drops.

## D-0039 — 2026-06-14 — founder + main architect (ETAPA-5b) — Structural budget recalibrated (document-engine honest 82M); escalations 16/17 resolved; finding-REGENERATION root surfaced (2nd instance) — architect contest-resolution rule + recurrence detector approved; codex gpt-5.5/xhigh proven in production

**Budget recalibration (D-0038 reroute aftermath):** resolving the two Fable-era escalations resumed the conveyor on opus; auth-access ran a full build→validate→dual-audit cycle and **document-engine's re-validation (proc 147, opus) passed (89 tests, 0 failing) but tripped `context_budget` → escalation 16**. Investigation corrected an architect hypothesis (Doctrine §21, self): the 82M was NOT mostly infra-waste — **33 successful runs = 81.4M genuine; 4 infra-failed runs = 0.56M (<1%)**. The stage genuinely cost ~5× a healthy structural one (a real spec contradiction + two infra outages forced abnormal re-derivation; raw-flow counting inflates across many iterations). So "discount infra-waste" (the first-proposed fix) would NOT clear it — abandoned. **Founder-approved: structural cap 75M→120M** (calibration bump; "eram încă la calibrare cu plafoanele"). Caps catch RUNAWAYS, not honest rework on an over-large stage. **Escalation 16 → rework:VALIDATE** (re-validate under the new cap; passes). The real structural lesson — **finer stage decomposition** (document-engine should have been 2-3 stages) — is the Opus-era trigger-gated response, applied to FUTURE stages, not the near-done one.

**Escalation 17 (auth-access `unresolved_contest`) — LIVE application of the new contest-resolution rule:** the rework cycle produced three findings: **AA-A2 [info] RECURRED** (round 1 id16 → round 2 id19) — the EXACT regenerating-finding pattern from CE-AUDIT-1, now the **second instance** (Doctrine §8 threshold for a mechanism). Resolved **rework:SPEC**: (1) AA-A2 → FIX THE SPEC TEXT (pin the idle-timeout set→unset behavior as accepted-as-is: strictly more-restrictive, self-healing) so the next audit stops raising it — the standing rule applied, not another deferral; (2) **F-AUTH-002 [major] OVERRULED** — admin device approve/revoke matches spec §5.10 verbatim; the `can_manage_devices` gate is on the DRF API plane (§3.2/§4.8/ADR-0002), the gap needs a non-superuser staff user not realizable in seeded v1, and the auditor's fix would contradict the founder-only-admin design; the defense-in-depth superuser-check question routed to the A4 human gate as an as-built note; (3) **F-AUTH-003 [low] COMPLY** — RO error text on the not_authenticated 403 (F1 §7), applied at BUILD after the spec amendment.

**Root surfaced (the bigger thing the founder kept probing):** CE-AUDIT-1 and AA-A2 are the same class — **accurate, adjudicated-no-action findings REGENERATE every audit** because (a) the architect deferred the spec fix as "editorial debt" (attention-based, regenerates), and deeper, (b) the finding-triage vocabulary `{comply, contest, duplicate}` has **no "adjudicated: accurate, no action, permanently closed" disposition**, so such findings are forced into `contest` → escalation → re-raised next round. The detector flags the symptom; the deeper fix is a new disposition that permanently closes. **Registered for founder decision (NOT built): which to build — the recurrence flag (symptom), the no-action disposition (root), or both.**

**Founder governance ruling (how a registration ensures mechanical return — Doctrine §20):** by TYPE. **Architect rules** → an architect-scoped prompt/canon (NOT the shared canon, which all agents get) so any-level architect always carries it; canon is segmented by inject-bundle (pipeline_agents/founder_facing) but the files are universal, so this needs a new architect bundle + an `architect-operations.md` file + the launcher's CANON_FILES. **Mechanism triggers** → a code predicate the orchestrator evaluates and surfaces on the dashboard when it trips. **Decision log / handoff watch-list** → traceability only; must POINT to a canon-rule or a code-predicate, never BE the guarantee. **Approved to build (next, as a focused unit):** (a) the architect contest-resolution rule in a new architect canon bundle; (b) the finding-recurrence mechanical dashboard flag.

**Codex gpt-5.5/xhigh proven in production:** auth-access's round-2 cross-model audit (proc 151, `auditor_cross_model`, codex gpt-5.5 + `model_reasoning_effort=xhigh`) ran exit 0 — the D-0038 codex reasoning routing works under real load, not just the smoke test.

**Deployed** (disarm→stop→start→re-arm, 0-agent window) carrying the 120M cap; escalations 16/17 then resolved; conveyor running — auth-access in SPEC (opus), document-engine re-VALIDATE (opus), 0 open escalations.

## D-0040 — 2026-06-14 — founder + main architect (ETAPA-5b) — Finding-regeneration program approved (option c); layered canon + architect rule BUILT/VERIFIED; launcher effort→max; 3 slices queued

**Context:** the finding-regeneration root (D-0039) — accurate-but-no-action findings loop because the triage vocabulary can't express them — drove a five-part founder-approved program. The founder also asked three sharp design questions, each answered before building.

**Founder Q&A (this session):**
- *After a documentary spec correction, does it rebuild?* Verified in code: `VALID_STAGE_TRANSITIONS[SPEC] = {BUILD, CANCELLED, ESCALATED}` — SPEC ALWAYS goes to BUILD; there is NO skip path. So a purely-documentary spec amendment (code-neutral) needlessly re-runs build+validate+audit. **Approved: a documentary-amendment path** (architect asserts code-neutral → SPEC→AUDIT, skipping BUILD+VALIDATE). [slice, queued]
- *The three options, clearly:* (a) recurrence flag = symptom (dashboard light, doesn't stop the loop); (b) no-action disposition = root (a 4th triage verb "accurate·acknowledged·closed" that records the finding settled so later audits don't re-raise it — also avoids the rework:SPEC rebuild); (c) both. **Approved: (c).** [2 slices, queued]
- *Canon segmentation for a both-role:* the old model picked ONE exclusive bundle per role — wrong for main_architect (founder-facing AND architect). **Answer + fix: canons COMPOSE by additive layers, not exclusive bundles.** A role gets the UNION of the layers that apply.

**BUILT + VERIFIED this turn (slice 1/4 — layered canon + architect rule + launcher):**
- Canon redesigned additive (D-0040): base bundle (pipeline_agents | founder_facing) + an ADDITIVE `architect` layer for `canon.architect_roles` = [main_architect, phase_architect, spec_agent], deduped/order-preserved. main_architect/phase_architect (both) → doctrine+conventions+founder_protocol+architect_ops; spec_agent → +architect_ops without founder_protocol; builders/validators/auditors → unchanged (the architect rule does NOT leak). New fields default `[]` (backward compatible).
- New canon file `work-protocols/architect-operations.md`: the founder-approved contest-resolution rule (fix the generating artifact, never defer as "editorial debt"; classify spec-wrong → amend text vs accurate-no-action → the disposition) + carry-the-WHY-into-rework. Lives in the architect layer ONLY (not the shared canon — the founder's explicit correction).
- `claude_canon.sh`: the Main-Architect session is both founder-facing and architect; its flat CANON_FILES already composes a union, so it gains architect-operations.md. **Default `--effort` xhigh→max** (founder directive) on both exec paths — effect: the NEXT architect launch (successor) runs at max with the rule; the current session is unaffected.
- Non-executor verifier (clean context, Doctrine §4): **APPROVE** — trust boundary airtight (architect_ops reaches exactly the 3 roles, zero leak to non-architect agents), no list mutation, dedup correct, launcher fail-fast covers the new file. **803 tests** (+1 composition test). Committed `69e583f`. NOT yet deployed — batches with the remaining slices' restart.

**Queued (slices 2-4, each design→build→non-executor verify→merge, then one consolidated deploy):** (2) **no-action finding disposition** — the root fix; needs care on HOW a later clean-context audit learns a finding was settled (feed settled findings into the audit prompt + auto-close matches at executor triage); touches scheduler audit-triage, the response schema, audit prompt, db. (3) **recurrence flag** — db query for an overruled/settled finding that reappears + dashboard surface (the mechanical backstop the founder asked for). (4) **documentary spec-amendment path** — a SPEC→AUDIT transition gated on an architect "code-neutral" assertion; touches VALID_STAGE_TRANSITIONS (a core contract — heaviest blast radius, build last with the fullest verification).

## D-0041 — 2026-06-14 — founder + main architect (ETAPA-5b) — Integration validator → opus (context overflow); launcher auto-Remote-Control (founder's pre-5c blocker); escalation-routing design discussed

**[19] document-engine `agent_run_failed` — root:** the stage reached the MERGE GATE (passed validate + dual audit + Tier-1!) but the **integration_validator (codex/gpt-5.5) ran out of context window** — NOT codex down (the codex AUDITOR succeeded minutes earlier). The merge-gate payload = full diff of the gating unit + ALL sibling diffs since contract freeze; it grows per merged sibling, and document-engine (4th to merge, after skeleton/config-registry/core-entities) was the first to exceed gpt-5.5's window. **Founder approved (a):** route `integration_validator` codex→**opus** (claude, 1M window — the hard requirement: the validator must FIT what it reviews). DoD §5.2 cross-model is PREFERRED not mandatory; cross-model diversity stays at the per-stage AUDIT (auditor_cross_model). Config changed, **NOT yet deployed** (awaiting a clean window). Scaling watch item: even 1M could be tight when all ~14 foundation stages merge → the durable fix is smarter diff-scoping (diffs touching shared contract surfaces only).

**Launcher auto-Remote-Control (founder's critical pre-succession blocker):** the founder is on his PHONE with no terminal — he could not run `/rc`/`/rename` in a successor session, so a successor would launch un-reachable. Fix: `claude_canon.sh` now passes **`--remote-control <name>`** (verified against the claude-code remote-control docs via the claude-code-guide agent: enables RC + sets the phone-visible name at launch, no interactive step; uses the CLI's existing claude.ai login). Name defaults to the tmux session, override `SFF5_RC_NAME`; `SFF5_NO_RC=1` opts out. Dry-run verified all three paths. session-succession.md updated: zero founder taps; VERIFY the successor's RC on the phone BEFORE the predecessor goes silent (predecessor's live RC is the fallback if the successor's RC silently fails).

**Escalation-routing design (discussed, founder "in principiu ok", NOT built):** the real mechanism today — a stage escalation fires an ntfy to the FOUNDER + dashboard, but there is **no phase-architect ACTOR**; `target=phase_architect` is a label with nobody home. The only auto-resolver is the capacity governor (limit-marked agent failures only). So technical escalations ping the founder (wrong resolver) and wait for him to relay to the architect, who has **no direct notification** (closed this turn with a background monitor — polls escalations/decisions/liveness, alerts the architect session). Simulation: a phase-architect resolver COULD handle a contest ([18]-type, given the finding+rationale+spec+history+the new architect rule) but NOT [19] (a config issue — its rework action set can't change model routing → proves routing is too coarse). **Agreed direction:** (1) classify escalations by resolver competence (stage-rework→phase, config/infra→main, product→founder); (2) guarantee an actor+action per level; (3) a **stuck-escalation detector** — open > threshold without resolution → auto-escalate UP a level + ntfy (the Doctrine §20 mechanical guarantee); (4) richer resolution actions (e.g. re-run only the failed gate). **Threshold proposed: T_architect ≈ 30 min** (when the architect is active the monitor resolves in minutes, so it rarely bumps; 30 min catches a truly-absent architect before the founder waits too long); product-level escalations ntfy immediately; the existing `decision_latency_alert: 24h` stays for founder product decisions. Founder confirmation of the 30-min value + whether to also build the auto-resolver-agent is PENDING. NOT automating escalation JUDGMENT — reliable notification + the stuck-detector + better routing, keeping human arbitration.

## D-0042 — 2026-06-14 — founder + main architect (ETAPA-5c) — Merge-gate re-entry verb built/verified/deployed; document-engine UNBLOCKED (DONE); auth-access §3.1 triage-isolation incident; escalation-routing founder-approved (human-arbiter + 30-min stuck-detector, persist-in-code)

**Succession executed:** ETAPA-5b → **ETAPA-5c** per D-0037 (session-succession.md). Context marker rewritten to 5c's session id (the D-0041 launcher pre-wrote it; confirmed). 5c on opus @ effort max, Remote-Control ON named ETAPA-5c, with the architect-operations canon layer. Own background monitor started (5b's died with its session; a 5b zombie monitor found still polling → killed).

**`rework:MERGE_GATE` (item 1 — the [19] root-respecting fix) — BUILT, VERIFIED, DEPLOYED:** [19] (document-engine `agent_run_failed`) was the Tier-2 integration_validator overflowing gpt-5.5's context at the merge gate (validate+dual-audit+Tier-1 had PASSED). The only resolution re-entering near the gate is `rework:VALIDATE` — which re-runs the structural validator (this stage burned 51M there) + dual audit (~32M), risks fresh non-deterministic audit churn, AND pushes document-engine's cumulative 107M past the 120M structural cap → guaranteed re-escalation. `scheduler.py:1230` already reserved a limit-killed Tier-2 "for the architect" with no auto-mapping, so we COMPLETED that vocabulary: `rework:MERGE_GATE` (STAGE_ESCALATION_RESOLUTIONS) + `ESCALATED→MERGE_GATE` (VALID_STAGE_TRANSITIONS) + a RO dashboard gloss. Re-enters ONLY the merge gate (Tier-1 rebase+suite + Tier-2 integration_validator) — ~1-3M not ~20M, no re-validate/re-audit. Builder (worktree) → independent adversarial verifier (clean context): **APPROVE**, mutation-tested (both new tests fail without the change); `_CAPACITY_RESOLUTIONS` confirmed UNCHANGED (MERGE_GATE deliberately absent — the auto-resolver must never re-enter a limit-killed Tier-2). Verifier surfaced a NON-BLOCKING foot-gun: the verb has no machine guard, so misapplying it to an `unresolved_contest` (strands a contested finding forever) or a pre-AUDIT escalation (bypasses structural validation + the §9 founder human gate on a critical stage) is dangerous. Per Doctrine §8 (no preventive machine guard without an incident) the guard is a CANON RULE: **architect-operations.md §3** — `rework:MERGE_GATE` ONLY for a merge-gate `agent_run_failed`. Commits 1161a8a (verb) + cc2570a (constraint); 805 tests.

**Consolidated deploy (one restart, clean 0-agent window):** carried the 4 pending committed changes — additive canon layer (69e583f), integration_validator codex→opus (b71bd6c), the merge-gate verb (1161a8a), the usage constraint (cc2570a). Ritual per first-live-run.md: disarm watchdog → C-c (0 agents) → fresh tmux `factory` (`.venv/bin/sf-factory run`, replicated PATH incl. nvm node) → verify (dashboard bound, recovery complete, scheduler loop, liveness fresh) → re-arm. Healthy. The clean window arrived exactly as predicted: auth-access escalated (below) → both active stages ESCALATED → 0 agents.

**[19] RESOLVED → document-engine DONE:** `resolve-escalation 19 rework:MERGE_GATE` → ESCALATED→MERGE_GATE → Tier-1 passed → integration_validator (confirmed `claude --model opus --effort xhigh`) reviewed the full payload, FIT, passed (tier2_gate) → MERGE_GATE→DONE. Reroute + verb proved end-to-end. document-engine's completion released the foundation DAG: dependency-cascade, media-attachments, print-pdf, register-schemas → SPEC.

**Incident — auth-access [20] `internal_error` (§3.1 triage-isolation):** during auth-access audit triage the executor COMPLIED with finding ASM-A1 but ALSO edited code in-place (errors.py +33, messages.py, build-notes.md, new test_errors.py) instead of only writing findings-response.json. The automatic comply→BUILD transition's `_assert_no_unregistered_files` (§3.1, scheduler.py:1939) correctly refused a dirty BUILD entry → IntegrityError contained at the executor boundary → ESCALATED (guard worked; no corruption). The restart's recover() dirty_worktree_reset (reset --hard + clean -fd) cleaned the tree; resolved `rework:BUILD` (re-fix ASM-A1 from committed audit context). ROOT: the triage step hands a full-tooled builder no "respond-only" boundary, and unlike the agent-FAILED path (`_discard_uncommitted`, 1576) the triage-SUCCESS→BUILD path has no stray-write cleanup. FIX folded into slice 2 (both touch `_step_audit` triage, §6): (a) _respond_prompt forbids code edits at triage; (b) commit triage artifacts + discard stray uncommitted before comply→BUILD (mirror _enter_awaiting_human + _discard_uncommitted) WITHOUT weakening §3.1. §8: first occurrence → logged + folded; expedite if it recurs before slice-2 deploys.

**Escalation-routing — founder DECISION (mode 1, after a full as-built walkthrough):** today the orchestrator writes an escalation row (trigger + a `target` label by nature/level — stage-conveyor→phase_architect, cross-cutting→main_architect) + ntfy to founder + dashboard + freezes the unit; but NO actor consumes `target` (resolution is human-via-CLI), the lone auto-resolver is the capacity governor, and the 24h re-alert is for founder DECISIONS not escalations → no resolution-latency guarantee. **Founder approved:** (1) human stays arbiter — NO autonomous resolver agent (don't automate judgment); build only the mechanical layer — live routing (actor+action per level) + a **stuck-escalation detector** (open > threshold → escalate up + ntfy) + richer resolution actions; (2) **30-min** threshold (configurable); (3) HARD REQUIREMENT — the architect must learn of an escalation within **≤5 min** (the monitor polls every 45s ≈ 1-2 min, meets it); (4) the mechanism MUST be CODE in the orchestrator so it **persists across ALL future main sessions** (vs the session-scoped monitor cârpă — the stuck-detector makes "nothing sits silently >30min" a factory law).

**Open / next:** slices 2-4 (2 = no-action disposition + the [20] triage fix; 3 = recurrence flag; 4 = documentary spec-amendment path) + the escalation-routing mechanism → one consolidated deploy at the next clean window (windows now rarer — conveyor busy post-DAG-release; batch). auth-access reworking ASM-A1; watch §3.1 recurrence. Zero open escalations at writing.

## D-0043 — 2026-06-14 — founder + main architect (ETAPA-5d) — Slice 2 (no-action `settled` + [20] triage fix) BUILT/VERIFIED/MERGED (deploy pending); escalations [21] held / [22] [23] resolved (durable root-fixes) / [26] open; capacity-governor explained + proven; budget-reset→architect notification gap (founder-flagged); robustness program DESIGNED+RULED

**Succession executed:** ETAPA-5c → **ETAPA-5d** per D-0037. Context marker rewritten to 5d's session id; 5c's session-scoped monitor found still polling → killed; own monitor started (open-escalation set + pending-decision set + orchestrator-liveness, exit-on-change). 5d on opus @ effort max, Remote-Control ON named ETAPA-5d, architect-operations canon layer.

**Slice 2 (finding-regeneration: `settled` no-action disposition + the [20] triage write-isolation fix) — BUILT, VERIFIED, MERGED (deploy PENDING).** Built per `docs/design-slice2-noaction-disposition.md` in 2 units, each by a worktree builder then an INDEPENDENT clean-context adversarial verifier (Doctrine §4): Unit A (migration `0002` widening `audit_findings.status` CHECK +`settled`; `models.STAGE_NOACTION_RESOLUTION`; `_step_escalated` settled special-case → settles contested findings then `_leave_clean_audit` risk-routes MERGE_GATE/AWAITING_HUMAN; `_audit_prompt` do-not-re-raise memory — **safety pin: status set `{settled,overruled}` ONLY**, mutation-tested; cli accepts `settled` stage-only) and Unit B ([20] fix: `_respond_prompt` write-boundary + UNCONDITIONAL `_discard_uncommitted` after the response commit, recorded into the transition payload; §3.1 `_assert_no_unregistered_files` NOT weakened). Verifiers APPROVED with evidence: the safety-pin test fails when the set is widened; migration `0002` applied to a COPY of the LIVE db preserved all 33 findings + index + accepts `settled`/rejects invalid; the [20] mutation reproduces the §3.1 IntegrityError without the discard. Merged to factory main (ea9ebd4 + 7c14f55) — **full suite 817 green on main**. The running orchestrator is unaffected (loaded code); migration `0002` + the new code apply at the next RESTART. **Deploy still pending a clean 0-agent window** — and the deploy is the churn-stopper (see [26]).

**Escalations this shift (the live finding-regeneration class slice 2 fixes — actively recurring post-DAG-release):**
- **[21] dependency-cascade `unresolved_contest` — HELD (not resolved).** Two `info` findings (a partly-tautological rollback test faithful to frozen spec §3.6; an uncovered defensive `NotImplementedError` branch) — accurate but no-action; executor's contest sound. The correct disposition is `settled`, which is built but NOT yet deployed. Resolving via the live stopgap `rework:VALIDATE` would re-validate+re-audit (~45M tokens) and almost certainly REGENERATE the same trivia (the exact loop). Decision (Doctrine §12): hold for the clean `settled` post-deploy. dependency-cascade is non-critical → `settled` routes MERGE_GATE→DONE.
- **[22] register-schemas `unresolved_contest` — RESOLVED `rework:BUILD`.** MIXED case: two REAL defects the executor complied with (F-RS-AUD-001 high — snapshot UniqueConstraint missing → nondeterministic balances, Doctrine §20; F-RS-AUD-002 medium — StockMovementSnapshot.own_pj nullability mismatch) + one contested info finding (RS-AUDIT-02). The resolution mechanics tightly couple route-to-BUILD with sustain-all-contested, so `settled` cannot serve a mixed case → `rework:BUILD` with a rich rework_context (build the two fixes; RS-AUDIT-02 is a DEFERRED cross-phase decision, do NOT implement). The deferred decision (snapshot acceleration axis-discriminator vs managerial-only-snapshots policy, owner = reporting Phase Architect) was REGISTERED in `docs/projects/erp/PROJECT.md` §2 reporting (48038c3) so it isn't buried in a foundation-stage artifact. Worked: 32/33 now `complied`/fixed.
- **[23] auth-access `contract_change_request` — RESOLVED `rework:SPEC` (durable root-fix).** First COMPLETE Tier-1 gate run (no prior actor ran it whole) → 9 RED: auth-access's DeviceRegistryMiddleware correctly 403s a sibling stage's (print-pdf) local DRF `api_client` fixture that attaches no device; spec §3.1's root-conftest override structurally can't reach a closest-conftest-shadowed local fixture. Builder STOPPED per §10 (no silent sibling patch) — correct discipline. Verified the crux: `device_for` exists ONLY on auth-access's branch, NOT on the integration base where print-pdf lives → a **circular dependency** (print-pdf's fix needs auth-access's device mechanism; auth-access can't merge until print-pdf's tests pass) → auth-access's branch is the only integration point holding both pieces. Routed `rework:SPEC` (not `rework:BUILD`): the spec is the generating artifact for the scope — amending §2/§3.1/§6.9 makes spec+code agree, avoiding an audit-regenerated scope contradiction (architect-operations §1). Directive: extend the device-test-integration to ALL API-test client fixtures (the durable option-c, realizing §3.1's own stated intent) + a permanent CONFORMANCE GUARD so future sibling stages can't reintroduce the seam (Doctrine §11/§20). NOTE: the founder's earlier `critical_stage` approval (decision 1) rested on a false "all gates green" premise — auth-access re-confirms at its gate on a now-GREEN full suite. auth-access advanced SPEC→BUILD.
- **[26] register-schemas round-2 `unresolved_contest` — OPEN at handoff.** The re-audit after [22]'s fix produced a fresh finding set; contested: RS2-SAME-02 (info) + F-RS-CROSS-001 (medium). Successor adjudicates per architect-operations §1 (the [22] method) — and note the deeper fix is deploying slice 2 (its audit-memory ends these regenerating re-raises).

**Founder interactions (mode 1/2/3):** (a) Explained the **capacity governor** (D-0037) as the usage-limit work-stopper — `enabled:true`, in the running orchestrator, and PROVEN today: budget wall hit 14:10 (`capacity_hold_started`, drained), resumed 14:51 (`capacity_hold_ended`, auto-resolved the limit-marked agent failures [24][25]), zero stages lost — distinct from the systemd `sf-factory-watchdog` which is a LIVENESS pager (founder-death ntfy), not a work-stopper. (b) Founder FLAGGED a real Doctrine §20 gap: at budget reset the ARCHITECT is not notified (session rate-limited on the same budget; monitor had exited at the wall and couldn't relaunch) — he had to relay it manually → now **UNIT 3** of the robustness design. (c) Advised the **dashboard redesign** (founder-requested, FUTURE): dedicated session, UX-first, MOBILE-FIRST (he drives by phone), after foundation/at a lull — registered as a task. (d) Explained [23]'s integration seam + the circular-dependency rationale (he probed whether reworking the cheaper sibling stage would be cheaper — it isn't: circular dep + red-integration-branch risk).

**Robustness / anti-silent-stall program — DESIGNED + architect-RULED, build PENDING** (`docs/design-robustness-antistall-14-06-2026.md`, code-grounded; unifies build-program items #3/#4/#9). UNIT 1 scheduler-fairness (no-spawn steps — ESCALATED resolutions, gates — exempt from the agent-slot cap via the existing `_step_spawn_roles` predicate; `continue` not `break`); UNIT 2 escalation routing + a tick-based stuck-detector (open >30min OR **resolved-but-unit-still-ESCALATED** >30min → bump `target` up the `phase_architect→main_architect→founder` ladder + page; pure DB predicates, NO autonomous resolver per D-0042) — and the orchestrator OWNS first-notification (`escalation_opened_notice` on first tick) making "architect learns ≤5min" a CODE law surviving a dead monitor; UNIT 3 budget-reset → architect page on `capacity_hold_ended`. Open Questions Q1-Q4 + unsure notes RULED (see the doc's "Architect rulings — ETAPA-5d"): one shared ntfy topic + distinct `[arhitect]` event/title (no second topic — D-0004); orchestrator-owns-first-notification; defer new granular actions + relabel-only at the founder rung; UNIT 3 page configurable default-on. **Successor's monitor must, once UNIT 2 ships, also grep `escalation_opened_notice|escalation_bumped|escalation_stuck_resolved`** (documented in architect-operations/session-succession as part of UNIT 2).

**Open / next (for ETAPA-5e):** (1) DEPLOY slice 2 at the next clean 0-agent window — the churn-stopper; then settle [21] `settled` + handle [26] + watch [23]'s re-confirm gate; confirm the cap-5 option with the founder before the restart. (2) BUILD the robustness program (UNIT 1→2→3, verify each) → batch into that deploy or the next. (3) Slice 3 (recurrence flag, depends on now-merged slice 2) + Slice 4 (documentary spec-amendment path, build LAST). (4) Dashboard-redesign is a FUTURE dedicated session. Open escalations at writing: [21] (held), [26] (open). auth-access BUILD ([23] fix), register-schemas ESCALATED ([26]).

## D-0044 — 2026-06-15 — founder + main architect (ETAPA-5e) — Robustness program (UNITs 1/2/3) + slice 2 + critical-budget 220M DEPLOYED; escalations [21] settled / [26][27] rework / [28] retried / [29][32][33] cycling; case-2b over-fire bug found in PRODUCTION + TEMP-silenced; codex + `-p`-billing contingencies (both currently OK)

**Succession executed:** ETAPA-5d → **ETAPA-5e** per D-0037. Context marker **birth-time-verified** (`stat -c %W`) — the predecessor's large `.jsonl` shares the mtime, so `ls -t` mis-identifies the session; I caught + corrected a wrong write. 5d's monitor killed, own started. 5e on opus @ max, RC ON named ETAPA-5e.

**Robustness program (UNITs 1/2/3) BUILT + INDEPENDENTLY VERIFIED + DEPLOYED** (`docs/design-robustness-antistall-14-06-2026.md`): each unit a worktree builder (`SF-F5-robustness`) → clean-context adversarial verifier (Doctrine §4), 840 tests green. UNIT 1 scheduler-fairness (no-spawn control-plane work exempt from the agent-slot cap; ACCEPTED transient `cap+K` rework-routing residual — bounded, pinned, economic-cap tolerance §8). UNIT 2 escalation routing + stuck-detector + orchestrator-owned first-notice (the ≤5-min code law). UNIT 3 budget-reset→architect-resume page. FF-merged to factory main with **slice 2** + **`budgets.per_stage.critical 150M→220M`** (founder-approved, for auth-access's honest broad-[23]-scope 156M burn; bounded — split if a critical stage exceeds 220M). **DEPLOYED 14-06 ~18:08** via the runbook ritual (disarm watchdog → C-c → fresh tmux → re-arm); migration 0002 auto-applied at `run` startup.

**Post-deploy escalations:** [21] dependency-cascade → `settled` (no-action → DONE); [26] register-schemas → `rework:BUILD` (build the F-RS-CROSS-001 `amount>=0` real-defect fix; RS2-SAME-02 stays deferred — the D-0043 [22] mixed method); [27] auth-access → `rework:BUILD` on the 220M budget. Earlier [28] status-notifications `internal_error` → `rework:VALIDATE` (transient retry). UNIT 1 confirmed live (resolutions picked up promptly, no starvation).

**⚠️ case-2b over-fire bug — found in PRODUCTION, TEMP-silenced (task #9).** Case (2b) ("resolved escalation + unit still ESCALATED") fired `escalation_stuck_resolved` for EVERY old resolved escalation of a unit re-ESCALATED for a NEW reason; its in-memory latch self-prunes on ESCALATED-oscillation → re-fires → ~32 false `[arhitect]` pages flooding the founder. The clean single-escalation verifier tests missed it; real first-use revealed it (§10). **Silenced** via `stuck_escalation_threshold_min: 999999` (disables case-2a/2b, keeps first-notice) + a 2nd quiet redeploy 15-06. **Fix pending (task #9):** scope (2b) to the unit's latest escalation / skip if an open escalation exists; restore threshold to 30; redeploy. (Two redeploy gotchas logged in the handoff: `pkill -f sf-architect-monitor.sh` self-matches the killer; startup >60s under the dashboard flood false-negatives a short up-poll — use a 150s log-marker check + verify manually.)

**Founder threads:** (a) **cap-5** (4 vs 5 agents) OPEN — default 4 applied. (b) **codex contingency** (task #10, until 15-06 09:00 UTC): if codex (`auditor_cross_model`, factory.config.yaml:42 — the SOLE codex role) fails, swap → a 2nd opus + restart ("2 separate opus controllers > 1; don't stop production"). (c) **`-p` billing scare** (15-06): founder feared a billing change cut `-p` credit — I TESTED `claude -p` directly = clean reply, exit 0; opus/codex agents exited 0 through ~21:34; the 14-06 19:43 capacity-probe failures (`credit`/`disabled`/`rate_limit`) were a transient usage-limit the governor auto-recovered. (d) buzz-flood acknowledged. Founder actively engaged + reachable; up late 14-15 June.

**Open / next (for ETAPA-5f):** (1) Resolve the 3 frozen escalations — [29] status-notifications + [33] register-schemas (context_budget), [32] auth-access (unresolved_contest). (2) **Fix case-2b properly (task #9) + restore threshold + redeploy.** (3) Standing: codex contingency + `-p` billing watch (until 09:00 UTC). (4) [23] auth-access re-confirm gate. (5) Slices 3/4 + monitor-event-update. Factory LIVE + QUIET (flood stopped), watchdog armed, 7 foundation stages DONE. Full operational detail: `docs/session-handoff-ETAPA-5e-15-06-2026.md`.

## D-0045 — 2026-06-15 — founder + main architect (ETAPA-5g) — case-2b fix + structural-200M/critical-250M caps CONSOLIDATED-DEPLOYED; escalations [29][33] rework:VALIDATE + [34] rework:MERGE_GATE; production flood ELIMINATED; agents stay 4

**Succession:** ETAPA-5e → 5f → **ETAPA-5g** per D-0037. 5f ran HEADLESS (RC-less launch bug — unreachable by founder), committed the case-2b fix (`4198f2c`, threshold 999999→30 + scoped detector), then EXITED on its own. 5g reconciled: confirmed NO second architect alive (no process/tmux/open-fd held the 5f session `2fceb54a`), birth-time-verified its own marker (`49af861d`), started its monitor. The headless-launch step (3) had nothing to stop — reported honestly, not invented (Doctrine §7).

**case-2b fix INDEPENDENTLY RE-VERIFIED (Doctrine §4) before deploy:** clean-context adversarial subagent — VERDICT PASS, 842/842 tests, scenarios a–g (N-old-resolved+1-open → (2b) ZERO; multi-resolved-no-open → ONCE; legitimate single-stuck still fires; per-unit GROUP BY isolation; (2a) climb unaffected; MAX(id)==most-recent via AUTOINCREMENT) + MUTATION sanity (neutralizing the skip line reproduced the over-fire → tests have teeth). Non-blocking note: "latest episode" = most-recently-OPENED (MAX id) — correct for a re-escalated unit.

**Two budget recalibrations (founder-approved, mode 1):**
- **structural 120M→200M (`17ff131`).** [33] register-schemas (~127M) + [29] status-notifications (~132M) re-tripped the 120M cap — genuine honest work (≈94% clean runs: 35/2 + 27/2 clean/infra-fail), near-done, the D-0039 document-engine pattern. Architect rec 160M; **founder chose 200M** (completion headroom).
- **critical 220M→250M (`48c019f`).** [34] auth-access NEW escalation: PASSED validate + dual-audit (incl. codex) + human re-confirm (decision 3, founder via dashboard 08:54 = the [23] gate, now-green suite) + merge-gate (Tier-1 `passed:true`, Tier-2 integration_validator exit 0), but the merge gate's own cost tipped it to 225.65M — 2.6% over 220M, a COMPLETION overage NOT a runaway. D-0044's "exceed 220M → split" bound targets runaways; splitting an approved, gate-passed stage is wrong. **Founder approved 250M.** BOUNDED: a FUTURE critical stage exceeding 250M → SPLIT.

**CONSOLIDATED DEPLOY** at the clean window (all 3 stages ESCALATED/frozen, downstream DAG-blocked → stable window): runbook ritual (disarm watchdog → C-c, orch died 2s → fresh tmux factory → scheduler-loop marker ~10s → re-arm). New orch pid 1146278. **Flood ELIMINATED in production: `escalation_stuck_resolved` since restart = 0** (vs ~32 in the 14-06 20:29–22:34 flood; only post-restart event = 1 expected first-notice re-notice for [34]). Window note: the favorable window arrived AS auth-access ESCALATED at [34] — founder had approved its human gate in parallel, advancing it MERGE_GATE→ESCALATED; the deploy-window watcher caught the 0-agent freeze.

**Resolutions (recalibrate→deploy→resolve order, D-0032 — confirmed no re-fire):** [33]+[29] → rework:VALIDATE (re-validate under 200M); [34] → rework:MERGE_GATE (re-enter ONLY the merge gate under 250M — no re-validate/re-audit/human-gate, architect-operations §3; re-runs integration_validator ~10-20M → ~240M < 250M → DONE). All 3 unblocked (auth-access→MERGE_GATE, register-schemas+status-notifications→VALIDATE); 0 open escalations; conveyor running.

**Founder threads:** **cap-5** → founder chose **stay 4**. **[23]** auth-access re-confirm → DONE (decision 3, approved on the now-green suite). codex CONFIRMED working (auth-access dual-audit passed); `-p` billing OK; the 09:00 UTC watch window expired clean.

**Open / next (rest of ETAPA-5g):** (1) babysit the running conveyor (auth-access → DONE, then downstream). (2) Slices 3/4. (3) monitor-event-update (exit on `escalation_opened_notice|escalation_bumped|escalation_stuck_resolved`) — now case-2b is live. (4) verify 5g RC on the founder's phone before any further succession (RC demonstrably working — founder drove this whole session). Operational detail: this session's transcript + `docs/session-handoff-ETAPA-5e-15-06-2026.md`.

## D-0046 — 2026-06-15 — founder + main architect (ETAPA-5g) — Structural budget 200M→250M (founder-directed); posting-engine merge-gate integration_validator 1M-context overflow (Part A deployed+falsified, Part B handed to 5h); built claude_redesign.sh (parallel discussion/redesign launcher); succession 5g→5h

**Structural budget 200M→250M (founder-directed, `d7482f0`).** status-notifications (status-layer + notification framework, large 2-subsystem structural stage) climbed toward the 200M cap mid-build; founder: "ridica la 250 la prima ocazie, altfel riscăm să pierdem lucrul." Proactive headroom so it doesn't stall near completion (the auth-access-at-merge-gate pattern). Committed; deployed in the batch below. Now structural == critical (250M). BOUNDED: split a structural stage exceeding 250M. (Reassured the founder: hitting a cap FREEZES the stage — work preserved — never loses it; status-notifications duly escalated [36] at 202M, frozen-safe, resolved rework:VALIDATE under the live 250M.)

**Built `claude_redesign.sh`** (founder-requested) — a scoped-down copy of claude_canon.sh launching a SEPARATE parallel session (tmux `redesign`, RC `Redesign`) for discussion + the factory-REDESIGN task, that does NOT manage the live factory (default scoping prompt forbids the marker, monitor, sf-factory mutations, orchestrator, factory.db, the `factory` tmux; read-only + design-on-a-branch allowed). Distinct identity via DEDICATED `SFF5_REDESIGN_TMUX`/`SFF5_REDESIGN_RC` env vars (NOT the live architect's `SFF5_TMUX_SESSION`/`SFF5_RC_NAME`) — a dry-run caught the live names leaking via the inherited environment (would have collided); fixed. Own `_assembled_canon_redesign.md` (gitignore widened to `_assembled_canon*.md`). Committed `206048f`.

**⚠️ posting-engine merge-gate integration_validator 1M-context OVERFLOW (incident, OPEN — handed to ETAPA-5h).** After the founder approved posting-engine's critical human gate (decision-4), its Tier-2 integration_validator (opus 1M) died HTTP 400 "prompt too long" (~1.05M tokens) → escalation `agent_run_failed` [35]. Root: `_tier2_prompt` feeds the FULL diff of the gating unit + ALL merged siblings (no total bound); at the 10th-merging unit the sibling bodies (~528k tok) are ~87% of the ~587k conversation. **Part A (tool allowlist, `419e554`, DEPLOYED):** `ModelRoute.tools` now accepts a list; `integration_validator: tools:[Read,Write]`. Hypothesis (subagent's token attribution): the 32 built-in tool schemas were ~half the prompt. **FALSIFIED by real first-use (§10):** the retry's prompt dropped only 1,052,242→1,035,884 (~16k) — `--tools` gates tool USAGE but does NOT strip definitions from context; the ~448k overhead is built-in system prompt + attachment content, not removable this way. posting-engine re-escalated [37]. Part A is harmless (genuinely the only tools it needs) — kept. **Part B (THE fix, handed to 5h):** render SIBLING diffs as hunk-headers (existing `worktrees.py _hunk_headers`) past a total budget in `_tier2_prompt` (+ phase twin), keeping the gating unit's full diff + contracts → conversation ~587k→~84k → ~532k total. Build → §4 verify → deploy → resolve [37] rework:MERGE_GATE; the RETRY is the verification (don't trust an estimate again). Spec in `docs/session-handoff-ETAPA-5g-15-06-2026.md`.

**Succession 5g→5h** per D-0037 (context guard at ~537k). ETAPA-5g's shift also (full record): reconciled ETAPA-5f (exited on its own — no 2nd architect), independently re-verified + consolidated-deployed the case-2b fix (flood eliminated, 0 false pages) + structural 200M + critical 250M (D-0045). Factory LIVE @ `419e554`, watchdog armed, foundation phase ~9 stages DONE. Handoff doc written; ETAPA-5h launched with RC; marker handed.

## D-0047 — 2026-06-15 — founder + main architect (ETAPA-5h) — posting-engine integration_validator overflow FIXED (Part B: sibling hunk-headers, `c97f8db`); deployed + dual-resolved [37]+[38]; RETRY-verified on BOTH stages; root cause confirmed general

**Succession context:** ETAPA-5h took the marker (birth-time-verified session `75265360`), started its monitor, read the 5g handoff + D-0045/0046. No leftover monitor to kill (5g's had exited). First and only task this segment: **Part B of the integration_validator 1M-context overflow** (the conveyor's one open blocker, [37]).

**MEASURED before coding (§10 discipline — the handoff's own lesson):** reconstructed posting-engine's REAL Tier-2 prompt via the actual `WorktreeManager`/`_tier2_prompt` against the live worktree (`phase/foundation`, freeze `7db099f`). Full-body prompt = **2,361,705 B**; siblings = 2,060,628 B (87%, 9 units); the 400's conversation token count = 587,469 → **~4.02 B/token** (the 5g handoff's "~528KB ≈ 528k-tok" was a bytes/tokens CONFLATION — at 4 B/tok a 2 MB threshold would NEVER have fired; measuring caught it). Header-mode prompt = 389,484 B (~97k conv tok → ~545k total w/ the ~448k fixed overhead the 400 itself reported).

**The fix (`c97f8db`):** new `process.tier2_max_total_bytes = 1_500_000`. When the assembled Tier-2 prompt would exceed it, sibling diffs render as file+`@@` hunk headers (`worktrees._hunk_headers`) instead of full bodies — gating unit's full diff + contracts + plan ALWAYS verbatim, and a `_SIBLING_ELISION_NOTE` tells the validator it is seeing changed regions (judge from those + gating diff + contracts; do not flag the elision). Both Tier-2 sites (stage `StageExecutor._tier2_prompt`, phase `PhaseExecutor._tier2`) routed through one shared `_render_sibling_diffs` helper. Threshold reasoning: triggers for posting-engine (2.36 MB); keeps full fidelity for conv ≤~375k tok (≤~823k total, ~178k margin); would need <2.72 B/tok to overflow (1.48× headroom over the measured 4.02). Part A (419e554 tool allowlist) kept — harmless.

**Verification (§4) BEFORE deploy:** independent clean-context adversarial reviewer → **PASS** (re-derived the threshold, fuzzed `_hunk_headers` against byte-truncated diffs = no exception/no body leak, confirmed both sites + no third site, mutation-checked the new unit test's teeth). Full suite **854/854**. New unit test `test_render_sibling_diffs_budget_switches_to_hunk_headers` (inclusive boundary + body-elision teeth).

**Deploy (clean window, runbook ritual):** both expensive stages frozen-safe → disarm watchdog → C-c (orch 1238808 died ~2s, db lock free) → fresh tmux `factory` → `recovery complete — entering scheduler loop` (new orch **1257468**) → re-arm watchdog → resolve. **During the window status-notifications' OWN merge gate hit the identical overflow → [38]** — which CONFIRMS the root cause is the general late-merging-unit pattern (Doctrine §11), not posting-engine-specific. Both `agent_run_failed`, both fixed by Part B → both resolved `rework:MERGE_GATE` (D-0032 recalibrate→deploy→resolve order; architect-operations §3 — they failed AT the gate after passing audit + human gate).

**RETRY = the proof (both stages, on the new code):** posting-engine integration_validator ran **5m32s** (13:38:28→13:44:00, not the ~2s 400) → `tier2_gate` findings `[]` (clean) → **MERGE_GATE→DONE**; conveyor auto-unblocked **fiscal-numbering PENDING→SPEC**. status-notifications validator ran **~7m11s** → `tier2_gate` **`SN-INT-001`** (severity low) → MERGE_GATE→BUILD. **Fidelity validated:** SN-INT-001 is a SUBSTANTIVE cross-unit finding (an unfulfilled rights-seam repoint owned by auth-access; `status_layers/rights.py`+`notifications/recipients.py` vs `accounts/rights.py`, exact lines + spec §7; "benign now, latent deny-bug for service-orders, tracked for v1 freeze; not a defect in the gating unit; not a merge blocker") — NOT a "can't-see-elided-diffs" complaint. Header-mode preserved enough fidelity for real integration reasoning, AND posting-engine got a confident clean review. **Falsifiability (§10):** header-mode is too lossy IF validators start raising elision/"missing diff" findings — they did NOT.

**Open / watch (ETAPA-5h):** (1) status-notifications now in BUILD over SN-INT-001 — a low, auth-access-owned, not-gating-unit finding. If it rework-loops (re-gate → same finding → BUILD), that is a **no-action disposition** candidate (architect-operations §1: accurate · acknowledged · the seam default is correct per spec §3.3) — the general monitor + recurrence flag will surface a loop. (2) Conveyor running: fiscal-numbering (critical, will hit the founder human gate), then integration-seed + ui-foundation. (3) Lower priority: slices 3/4; monitor event-update (D-0042). Working-mode note: `pgrep -af` on factory agents dumps the full canon into context — use sqlite/narrow `ps` instead.
