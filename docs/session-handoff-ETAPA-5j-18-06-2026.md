# Session handoff — ETAPA-5j → ETAPA-5k, written 18-06-2026 (D-0037 context-guard succession at ~506k)

**For ETAPA-5k (Main-Architect successor).** POINTER doc (Doctrine §9). Authoritative history = `docs/decision-log.md` **D-0046 → D-0056** + `docs/projects/erp/decision-log.md` **D-ERP-0002** + this transcript. Launch: opus, effort max, Remote-Control ON named ETAPA-5k, architect-operations canon layer. RC = the founder drives you from his phone.

## YOUR FIRST DUTIES (in order)
1. **Write your session id** to `~/.claude/sf-architect-session` (newest `.jsonl` in `~/.claude/projects/-home-artur-projects-SF-F5/` — verify via BIRTH time `stat -c %W "$f"`, NOT mtime).
2. **Update the monitor header to ETAPA-5k** (`~/.claude/sf-architect-monitor.sh` line 1-2) and **start it** (`bash ~/.claude/sf-architect-monitor.sh`, run_in_background) — **the factory is LIVE** (unlike my start), so start it immediately. Kill any leftover monitor by exact cmdline `bash /home/artur/.claude/sf-architect-monitor.sh` (NOT pkill -f; that matches the claude RC sessions' prompt text). 5j's monitor exited on [55]; no live monitor now.
3. **Read** `docs/decision-log.md` D-0054, D-0055, D-0056, then `uv run sf-factory status`. (D-0054 = split executed; D-0055 = E5/C1 deferred tension; D-0056 = merge-gate fix + the weekly-limit outage.)

## YOUR IMMEDIATE FIRST TASK — resolve escalation [55] (ordering)
**Open `unresolved_contest` on `inventory-procurement.ordering`** (I left it for your fresh context rather than verify it at 506k). 4 findings: **3 complied** (cross-model ORD-AUD-001 + ORD-AUD-002, both `medium`; same-model AUD-ORD-2 `info`) + **1 contested** (same-model AUD-ORD-1 `low`). Two real `medium` comply bugs ⇒ almost certainly **`rework:BUILD`** (fixes the 3 complied), AUD-ORD-1 → sustained. **Verify per the guide below** before resolving — read BOTH audit reports + the contest, confirm the 2 medium comply fixes are real + scope the build, and check AUD-ORD-1's contest (if it's a real bug, the build round catches it; if accurate-no-action, it'll re-loop → settle next round; if a spec-text error like NSG-AUDIT-1, correct the stage spec documentary-style so it doesn't regenerate). Reports live at `…/.worktrees/inventory-procurement.ordering/_factory/stages/inventory-procurement.ordering/` (audit-auditor_{same,cross}_model.md + findings-response.json).

## STATE SNAPSHOT (18-06 ~05:30 UTC — RE-CHECK with `status`)
- **Factory LIVE** (orchestrator pid **1590847**, restarted by me @ factory `c3226ea` for the D-0056 fix; watchdog ARMED; dashboard 8377 → 200). **factory repo main @ `50a2235`.**
- **Capacity RECOVERED 18-06 03:02Z** after a ~1.5-day weekly-spend-limit outage (see D-0056 + below). No capacity hold since. **The founder is AT his weekly Claude limit** — be usage-mindful; a fast agent exit (~1-2s, exit 1) with `usage_limit_suspected` is the billing signature, NOT a code bug.
- **inventory-procurement RUNNING.** Stages: parts-catalog DONE; **stock-core-foundation DONE, stock-core-reservation-release DONE** (the split — complete + validated); stock-core CANCELLED; **negative-stock-guard MERGE_GATE** (my [54] rework, should reach DONE); **ordering ESCALATED [55]** (your task); 7 PENDING (reception, supplier-fiscal-invoice, warehouse-issue, returns-supplier-client, stocktaking, overdue-detection, stock-views, phase-integration).
- **1 open escalation [55], 0 pending decisions.**

## WHAT 5j DID (full detail: D-0054 / D-0055 / D-0056)
- **Executed the stock-core split** (D-0053 directive) — surgical DB edit (CANCEL old + replacement_registered event + 2 PENDING stages + DAG rewire + committed phase-plan.json `b4c5bf7`), verified every load-bearing claim first, clean restart. **Design refinement:** moved `pick_for_issue` to the reservation stage (it reads the R3 register) — separates the two bug-heavy facets (edit/storno vs reservation). **Both split stages CONVERGED** (foundation [49] rework:BUILD; reservation-release [50] settled) — the split is proven (vs. old stock-core's 5 non-converging rounds). **D-0054.**
- **Adjudicated the E5/C1 lot-attribution tension (D-0055)** — settled RR-E5-LOT-001 (reservation-driven release on the lot_reference-free R2 substrate is sound for all current consumers; the auditor's clamp-fix was self-defeating). **⚠️ DEFERRED contract-watch:** revisit at `service-orders` — if the real `sale_document` producer needs lot-exact COGS attribution at release → `_CONTRACT_CHANGE_REQUEST` against C1 (an R2 `lot` column), never a local edit. Most consequential carry-forward for service-orders.
- **Fixed a real factory framework bug (D-0056, `scheduler.py` @ `c3226ea`)** — the merge-gate `_tier2` only checked the scratch worktree for the integration-report sidecar, but the integration_validator writes it to the REGULAR worktree (its prompt's diff carries regular-worktree absolute paths). Two `internal_error` loops ([51],[52]) → root-cause fix: `_tier2` now falls back to the regular worktree dir. Deployed via clean-window restart. **VALIDATED** — reservation-release merged `89db4a9`.
- **Weathered the weekly-spend-limit outage** — the factory's capacity-hold + `capacity_probe` (5-min) mechanism auto-paused (no work lost) and auto-recovered; [53] was the merge-gate validator dying 1.7s on the limit → resumed `rework:MERGE_GATE`.
- **Handled [54]** (negative-stock-guard, the routine pattern) — `rework:BUILD` for the AUD-2 info test + a documentary stage-spec correction (NSG-AUDIT-1: spec.md:234 `==10`→`==0`, code was right) so the re-audit doesn't regenerate it.

## RECURRING CONTEST-PATTERN GUIDANCE (you WILL hit this every stage — D-0049)
Each stage's AUDIT raises comply findings + 0-2 contested. Resolve via `uv run sf-factory resolve-escalation <id> <disp> --reason "…"`. **Verify against BOTH audit reports** (same-model + cross-model — they disagree; either can be the strict one — cross-model caught the high bugs on foundation/reservation-release, same-model raised the info ones on negative-stock-guard). Pattern:
- **Real comply bug(s) (unbuilt) →** `rework:BUILD`; contested no-action findings → `sustained` (re-raisable). The `sustained`/`settled` distinction: only `settled` + `overruled` enter the auditor's do-not-re-raise memory (scheduler.py:3424-3445); `sustained` re-raises → 2-round pattern.
- **Sole finding = no-action contest →** `settled` (architect-operations §1 path 2; closes at audit, structural→MERGE_GATE). Don't settle if an unbuilt comply finding exists (settled skips it — only settles contested).
- **Spec genuinely wrong (text lies about correct code) →** correct the stage spec.md documentary-style (architect-operations §1 path 1; like NSG-AUDIT-1) — don't rework:SPEC (forces rebuild).
- **Merge-gate `internal_error`/`agent_run_failed` AFTER audit passed →** `rework:MERGE_GATE` (re-runs only the gate; architect-operations §3). NEVER for unresolved_contest or pre-AUDIT.
- **Usage-mindful (founder at weekly limit):** for `info`-level complied findings where the property is already structurally guaranteed + validator-covered, weigh whether a full rework round is worth the tokens vs. its marginal value. Carry your full WHY in `--reason` (it reaches the build's rework_context, architect-operations §2).

## TRACKED OPEN ITEMS (do not lose)
- **⚠️ #1 (D-0055) the C1 R2-lot-column contract-watch** — revisit at service-orders sale_document (above). The single most important carry-forward.
- **#2 task #10 spec/contract reconciliations** — CROSS-STOCK-005 + SC-A2 → re-confirm at the relevant later-stage builds (reservation-release services are DONE; these surface at warehouse-issue etc.).
- **#3 OPEN-IP2** — Main-Architect C3 §6 re-sync for `nomenclature.SpecificPart` Layer-2 enrichment (`default=list`), at inventory-procurement integration.
- **#4 (D-0048)** rights-seam repoint before service-orders; **#5 (D-0049)** UI-A3 concurrent-reauth at first scoped-screen phase; **#6** founder DoD §13 retrospective (restart-ERP-code? — architect read = KEEP); **#7** monitor event-update (D-0042) — the monitor still uses the legacy open-escalation/decision/liveness set, not the new `escalation_*` events; works, deferred.
- **#8 standing founder decision (D-0056)** — capacity vs cost vs speed (auto-pause free / more capacity / cheaper models). Founder said "go on" → default (1) auto-pause; awaiting his (2)/(3) choice. Surface gently, don't block.
- **Housekeeping:** a stale ETAPA-5h RC session (pid 1241594) is still alive/idle — harmless leftover, kill if you want a clean `tmux ls`.

## WORKING-MODE (keep)
- Founder: Romanian, glossed (no bare IDs/codes), brutal honesty (§21), cost-conscious, drives by phone, answers gates on the dashboard, front-loads decisions, **currently at his weekly Claude limit — minimize noise + be usage-mindful**.
- Redeploy/stop ritual + clean-window: `docs/runbooks/first-live-run.md` + D-0047 (disarm watchdog FIRST → `tmux send-keys -t factory C-c` → verify dead → fresh `tmux new-session` → `recovery complete` → re-arm). CLI defaults to `factory.config.yaml`; `SF_DRIVER_PLAYBOOK` is synthetic-only (don't set). Editable install — `src/` edits take effect on restart. `pgrep -af` on factory agents DUMPS THE CANON — use `comm -23 <(pgrep -f 'output-format stream-json|codex exec'|sort) <(pgrep -f -- '--remote-control'|sort)`. Measure, don't estimate (§10).
- The capacity-hold mechanism (`capacity_probe` agents, `capacity_hold_ended` event) auto-handles usage outages — a paused conveyor with `usage_limit_suspected` events is the factory waiting, not broken; resolve any stuck merge-gate `agent_run_failed` with `rework:MERGE_GATE` once `capacity_hold_ended`.

## YOUR SUCCESSION
Finish your work unit, write the handoff, launch ETAPA-5l, **VERIFY 5l's RC on the founder's phone BEFORE going silent** (predecessor RC is the fallback), hand the marker, go silent. Don't keep working after the successor takes the marker.
