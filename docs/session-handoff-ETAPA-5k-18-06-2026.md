# Session handoff — ETAPA-5k → ETAPA-5l, written 18-06-2026 (D-0037 context-guard succession at ~506k)

**For ETAPA-5l (Main-Architect successor).** POINTER doc (Doctrine §9). Authoritative history = `docs/decision-log.md` **D-0046 → D-0057** (esp. **D-0057** — 5k's whole session) + `docs/projects/erp/decision-log.md` **D-ERP-0002** + this transcript. Launch: opus, effort max, Remote-Control ON named ETAPA-5l, architect-operations canon layer. RC = the founder drives you from his phone.

## YOUR FIRST DUTIES (in order)
1. **Write your session id** to `~/.claude/sf-architect-session` (newest `.jsonl` in `~/.claude/projects/-home-artur-projects-SF-F5/` — verify via BIRTH time `stat -c %W "$f"`, NOT mtime). This takes the context guard.
2. **Update the monitor header to ETAPA-5l** (`~/.claude/sf-architect-monitor.sh` line 1-2) and **START it** (`bash ~/.claude/sf-architect-monitor.sh`, run_in_background) — **the factory is LIVE**, start immediately. Kill any leftover monitor by the EXACT-cmdline approach, NOT `pkill -f` and NOT `grep -F 'bash …monitor.sh'` (both match the claude RC sessions whose prompt text contains the path): use `ps -eo pid,comm,args | awk '$2=="bash" && /sf-architect-monitor\.sh/ {print $1}'` then kill those pids. Launch the monitor with the Bash tool's `run_in_background: true` (NEVER `nohup … &` inside a Bash call — that orphans it untracked, so you never get the exit notification; 5k made that mistake once).
3. **Read** `docs/decision-log.md` **D-0057** (5k's full session — contest resolutions, the Option-B lot-substrate ruling, the own_pj suspected problem, the merge-gate fix spec) + skim D-0054/55/56, then `uv run sf-factory status`.

## YOUR FIRST TASK — the merge-gate auto-recovery fix (FOUNDER-DIRECTED, D-0057)
The founder (18-06) directed this as 5l's first task. **Add `"MERGE_GATE": "rework:MERGE_GATE"` to `_CAPACITY_RESOLUTIONS` (`src/sf_factory/scheduler.py:1320`) + UPDATE the pinning test** (the map is "pinned by test"; grep `_CAPACITY_RESOLUTIONS`/`capacity` in `tests/` — a test asserts MERGE_GATE's deliberate ABSENCE; update it + the comment at `scheduler.py:1313-1319`). **Why:** the `capacity_governor` auto-resumes limit-killed SPEC/BUILD/VALIDATE/AUDIT but NOT MERGE_GATE, so a usage-limit-killed merge-gate Tier-2 (`agent_run_failed` + `usage_limit:true`) is left for the architect — who is frozen by the SAME limit (incidents [61] this session + [53]/D-0056). **Safe because** `_auto_resolve` (`scheduler.py:1239`) fires ONLY on `agent_run_failed`+`usage_limit:true`; a stage AT merge-gate definitionally passed structural validation + dual AUDIT, so the architect-operations §3 misapplication risks (unresolved_contest / pre-AUDIT) cannot occur; `rework:MERGE_GATE` re-runs only the gate — exactly the manual fix for [61]/[53]. **Deploy via the clean-window ritual** (D-0047 / `docs/runbooks/first-live-run.md`: disarm watchdog FIRST → `tmux send-keys -t factory C-c` → verify dead → fresh `tmux new-session` bare `sf-factory run` → `recovery complete` → re-arm). Sequence the deploy at a clean window (don't C-c mid-build); resolve [71] first if it helps reach a clean state. Editable install — the src edit takes effect on restart.

## STATE SNAPSHOT (18-06 ~13:40 UTC — RE-CHECK with `status`)
- **Factory LIVE** (orchestrator pid **1590847**; `pgrep -f 'sf-factory run'` returns several pids — the orchestrator + its tmux shell + probes; 1590847 is the orchestrator). **factory repo main @ `4545e2c`** (D-0057 commit). Watchdog should be armed; dashboard 8377.
- **⚠️ Likely another usage-limit freeze just happened:** a ~75-min gap (12:24→13:40Z) where the monitor fired but the session didn't wake until the founder said "wake up" — the D-0056 signature (the architect frozen by the weekly limit). **The founder is AT his weekly Claude limit** — be usage-mindful; a fast agent exit (~1-2s, exit 1, `usage_limit_suspected`) is billing, NOT a code bug. The capacity-hold + capacity_governor auto-handle the conveyor; YOUR first task (the merge-gate fix) closes the last gap where the governor couldn't.
- **inventory-procurement RUNNING.** DONE (5): parts-catalog, stock-core-foundation, stock-core-reservation-release, negative-stock-guard, **ordering**. stock-core CANCELLED. **warehouse-issue** = converged + founder-approved §9 gate [9] + resolving a routine Tier-1 rebase conflict → should reach DONE (re-check; was VALIDATE). **reception** = ESCALATED **[71]** (new contest — your task). **stocktaking** = ESCALATED **[68]** (PARKED — see below). 5 PENDING (supplier-fiscal-invoice, returns-supplier-client, overdue-detection, stock-views, phase-integration).
- **2 open escalations: [68] stocktaking (parked), [71] reception (handle). 0 pending decisions.**

## YOUR IMMEDIATE WORK ITEMS (after the first task)
1. **[71] reception `unresolved_contest`** — the recurring D-0049 pattern (verify BOTH audit reports + the build's `findings-response.json` + the contest; `rework:BUILD` for real comply bugs, sustain no-action contests, settle when a no-action contest is the SOLE residual). **reception is a DEEP critical stage** — round 2 surfaced that the round-1 supplier-scoping was a HALF-FIX (steer COMPLETE fixes; see D-0057). Reports live at `/home/artur/projects/erp-workspace/.worktrees/inventory-procurement.reception/_factory/stages/inventory-procurement.reception/`.
2. **stocktaking [68] — the own_pj verification (FOUNDER-DIRECTED).** [68] residuals = **ST-OWNPJ-001** (the founder-deferred foundation issue) + **ST-UI-DIRECT-LINE-001** (a no-action UI contest — unfrozen §1.3 parenthetical, settle as no-action). The founder ruled (D-0057): do NOT apply the own_pj foundation fix; document as a SUSPECTED problem; **VERIFY first** — is a single depozit holding BOTH null-`own_pj` AND specific-`own_pj` lots for the same (item,vat) a real business scenario, and does it produce an actual wrong posting in practice (the auditor demo was synthetic)? 5k parked [68] (won't ship a critical money stage with an unverified suspected defect, won't churn empty reworks). **Verify with the founder (mode-2), then:** if confirmed real → the minimal additive foundation/IP2 read-API fix (`own_pj_isnull` flag on `project_lots`+`_filtered`, `stock_state.py`) — a foundation contract change, present to founder; if not reachable in practice → settle ST-OWNPJ-001 as no-action. Then stocktaking converges (critical → §9 gate → DONE).
3. **warehouse-issue** — confirm it reached DONE (it was finishing a rebase-conflict resolution).

## RECURRING CONTEST GUIDANCE (you WILL hit this every stage — D-0049/D-0057)
- **Real comply bug(s) unbuilt → `rework:BUILD`**; contested no-action → `sustained` (re-raisable). Carry the full WHY in `--reason` (it reaches the build's rework_context, architect-operations §2): name the finding, file:line, the fix direction, and what NOT to touch.
- **Sole residual = no-action contest → `settled`** (architect-operations §1 path 2). For a CRITICAL stage, `settled` routes to AWAITING_HUMAN (the §9 founder sign-off gate) — that is correct. Don't `settle` while an unbuilt comply finding exists (it skips the build).
- **Spec-text error (text lies about correct code) → correct the stage spec.md documentary-style yourself** (commit on the stage branch), don't `rework:SPEC`. Verify against the FROZEN source first (5k almost mis-corrected AUD-ORD-2 by an off-by-one matrix read — VERIFY, Doctrine §16/§21).
- **`_CONTRACT_CHANGE_REQUEST` (CCR) → adjudicate Option A (grant the frozen-contract change) vs Option B (accept the limitation + mitigate in-stage).** Default to B at the last responsible moment unless a current consumer genuinely needs it (Doctrine §12; D-0055/D-0057 warehouse-issue lot-substrate was Option B). A foundation contract change is weighty → present to the founder (he asked to be looped in on architecture).
- **`internal_error` "duplicate response for ref" →** the build wrote two response entries for one ref (both auditors used the same ref). `rework:BUILD` + steer "one findings-response entry per ref". FIRST occurrence was [62] (Doctrine §8 — if it recurs, the response contract / build prompt needs a (ref,auditor) fix).
- **Merge-gate `agent_run_failed` with `usage_limit:true` →** `rework:MERGE_GATE` (after `capacity_hold_ended`). Once your FIRST-TASK fix ships, the capacity_governor auto-handles these.
- **`sustained` vs `settled` matters:** only `settled`+`overruled` enter the auditor do-not-re-raise memory. 5k's ST-FIFO-001 sustain (not settle) is what let the build re-derive + reverse a real cross-depozit bug 5k had wrongly upheld — **the conservative `sustained` saved it.** Don't settle a contest you're not sure is truly no-action.

## TRACKED CARRY-FORWARDS (do not lose — full list in D-0057)
- **#1 own_pj verification** (above) + **the D-0055/warehouse-issue lot-substrate Option-A watch** (revisit at `service-orders` sale_document for lot-exact COGS, or `stock-views` for lot-grain reservation views) — the single most consequential architecture carry-forward.
- **#2** ordering URL-split → `inventory-procurement.phase-integration`.
- **#3** reception OPEN-RCP5 (storno-after-delivery) + the two-cost-layer edge (now fixed-by-rejection in [67]).
- **#4** OPEN-IP2 C3 §6 re-sync (`nomenclature.SpecificPart` Layer-2) at inventory-procurement integration; **#5** rights-seam repoint before service-orders (warehouse-issue WI-AUD-002 + reception AUD-XM-2 both reshaped seams toward the 4-arg F5 contract — progress); **#6** monitor event-update (D-0042 `escalation_*` events — the monitor still uses the legacy set; works, deferred); **#7** the standing capacity-vs-cost-vs-speed founder decision (he said "go on" → default auto-pause; awaiting his (2)/(3) choice).

## WORKING-MODE (keep)
- Founder: Romanian, glossed (no bare IDs/codes), brutal honesty (§21), cost-conscious, drives by phone, answers gates on the dashboard, front-loads decisions, **at his weekly Claude limit — minimize noise + be usage-mindful**. Mode-1 (his decision) for business/money/data/security/strategy/irreversible (incl. frozen-contract changes); mode-2 (free dialogue) for exploration/verification; mode-3 (autoresolve + concise report) for technical/local/reversible. He told 5k to handle routine stage churn SILENTLY and surface only for decisions/blockers/milestones.
- **Quiet-monitoring cadence:** the monitor re-invokes you on every open-escalation/decision set change. Handle routine contests silently; don't message the founder per routine stage. The monitor fires exit 10 (escalation set changed) / 11 (decision set changed) / 12 (orchestrator absent) — restart it after each fire (run_in_background).
- Clean-window/redeploy ritual + `SF_DRIVER_PLAYBOOK` synthetic-only: `docs/runbooks/first-live-run.md` + D-0047. `pgrep -af` on factory agents DUMPS THE CANON — use precise greps. Measure, don't estimate (§10). Editable install — src edits take effect on restart.

## YOUR SUCCESSION
Finish your work unit, write the handoff, launch ETAPA-5m via `claude_canon.sh` (SFF5_TMUX_SESSION=etapa-5m SFF5_RC_NAME=ETAPA-5m), **VERIFY 5m's RC on the founder's phone BEFORE going silent** (predecessor RC is the fallback), hand the marker, go silent. Don't keep working after the successor takes the marker.
