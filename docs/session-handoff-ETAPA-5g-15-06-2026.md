# Session handoff — ETAPA-5g → ETAPA-5h, written 15-06-2026 (succession per D-0037)

**For ETAPA-5h (Main-Architect successor).** POINTER doc (Doctrine §9). Authoritative history = `docs/decision-log.md` **D-0045 + D-0046** (ETAPA-5g's shift) + this session's transcript. You launch on opus @ effort max, Remote-Control ON named ETAPA-5h, architect-operations canon layer. RC = the founder drives you from his phone.

**Why this handoff:** ETAPA-5g ran a long, heavy shift (reconciled 5f, deployed case-2b + two cap raises, built the redesign launcher, drove the posting-engine integration-overflow saga). Context hit ~537k (>500k guard). Factory is LIVE + SAFE. The ONE open conveyor blocker — posting-engine's merge-gate integration_validator context overflow — is fully diagnosed; **your first real work is Part B of that fix** (spec below). Don't hand off mid-slice; this IS a clean boundary (the blocker is frozen-safe + well-specified).

## YOUR FIRST DUTIES (in order)
1. **Write your session id** to `~/.claude/sf-architect-session` (newest `.jsonl` in `~/.claude/projects/-home-artur-projects-SF-F5/` — **verify via BIRTH time `stat -c %W`, NOT mtime**; the predecessor's large `.jsonl` shares the mtime).
2. **Start your monitor** (`bash ~/.claude/sf-architect-monitor.sh`, run_in_background). Kill any leftover by **exact cmdline match** (NOT `pkill -f 'sf-architect-monitor.sh'` — that string is in your own kill command + your RC prompt → self-match; match processes whose full cmdline == `bash /home/artur/.claude/sf-architect-monitor.sh`). Update the script header to ETAPA-5h.
3. **Read `docs/decision-log.md` D-0045 + D-0046**, then `uv run sf-factory status`. (No harness TaskList carries over — rebuild your backlog from this doc.)

## State snapshot (15-06 ~13:00 UTC — RE-CHECK via status)
- **Factory LIVE on main @ `419e554`** (case-2b fix + structural 250M + critical 250M + integration_validator tool-allowlist). Orchestrator pid was 1238808 (re-check), watchdog ARMED, flood DEAD (case-2b verified: 0 stuck_resolved since deploy). The dashboard auto-commits founder decisions to main (`decision N: answer recorded via dashboard`) — main is shared between your commits + the dashboard's; **FF-merge only, NEVER rebase**.
- **Foundation phase nearly done.** DONE: skeleton, config-registry, core-entities, dependency-cascade, document-engine, media-attachments, print-pdf, register-schemas, **auth-access (critical, founder-approved)**. ~9 DONE.
- **⚠️ posting-engine: ESCALATED [37] `agent_run_failed`** — the merge-gate Tier-2 integration_validator overflows opus's 1M context (~1.05M tokens). **Your #1 task — Part B fix below.** Frozen-safe (logic passed validate + dual-audit + Tier-1 + founder gate decision-4; only the cross-unit integration review can't fit). Blocks downstream (fiscal-numbering, integration-seed, ui-foundation PENDING — depend on posting-engine).
- **status-notifications: VALIDATE** under the live 250M cap (~202M, re-validating after [36] resolved). Should complete; watch it doesn't re-trip (250M gives headroom).

## ⚙️ #1 TASK — posting-engine [37] integration_validator overflow, PART B (the effective fix)
**Diagnosis (done + empirically confirmed):** the integration_validator (`factory.config.yaml` route, opus 1M, mode print) builds its Tier-2 prompt in `scheduler.py _tier2_prompt` (~line 3444) feeding the FULL diff of the gating unit + **the FULL diffs of ALL merged sibling units** (no total bound; per-unit cap `process.tier2_max_diff_bytes_per_unit`=300KB). At posting-engine (10th-merging unit) that's ~528KB→~528k-tok of sibling bodies = ~87% of the ~587k conversation; total ~1.05M.
- **Part A (DONE, DEPLOYED 419e554, but INEFFECTIVE):** per-role tool allowlist — `integration_validator: tools: [Read, Write]`. Empirical result: prompt only dropped 1,052,242→1,035,884 (~16k). **`--tools` does NOT strip tool-definition bloat from context** (CLI loads defs regardless of the allowlist; the ~448k "overhead" is built-in system prompt + "attachment content" + defs, not removable this way). Part A is harmless (genuinely the only tools it needs) — LEAVE IT, build Part B on top.
- **Part B (DO THIS):** in `_tier2_prompt` (~3444) + the phase-level twin (~4114), when the assembled prompt exceeds a new total budget (`tier2_max_total_bytes`, e.g. ~2_000_000 B), render the SIBLING diffs as **hunk-headers** (existing battle-tested helper `worktrees.py _hunk_headers` ~line 627, already used for the Tier-1 conflict payload) instead of full bodies — KEEP the gating unit's full diff + contracts + plan verbatim. Cuts conversation ~587k→~84k → total ~532k, comfortable under 1M. Graceful: full sibling bodies for small phases, headers only when over budget.
- **Fidelity note (founder-transparent):** for over-budget phases the cross-check sees sibling changed-REGIONS not full bodies — a small, bounded reduction; the gating unit + contracts stay full, and the per-stage dual-audit already covers each unit. Acceptable per §11 (the alternative — can't merge large stages — is worse). The founder is OK with the approach (told 15-06).
- **Build → INDEPENDENT clean-context verify (Doctrine §4) → deploy (clean window) → resolve [37] `rework:MERGE_GATE`.** VERIFY by the RETRY: confirm the integration_validator RUNS (exit 0, minutes-long, not the ~2s 400). The retry IS the proof — Part A's retry falsified it; don't trust a token estimate again (my §10 lesson: real first-use falsified Part A).
- If Part B still overflows (unlikely — huge margin): investigate the ~448k overhead (what is "attachment content" / the pipeline-agent canon bundle — maybe bigger than the 1.9k I assumed).

## Other work items (priority order)
2. **status-notifications** — let it finish under 250M (watch for re-trip; it shouldn't).
3. **Remaining foundation stages** (fiscal-numbering, integration-seed, posting-engine downstream) unblock once posting-engine is DONE — normal conveyor; critical stages hit the founder human gate (surface like auth-access).
4. **Slices 3 & 4** (finding-regeneration program) — design work, lower priority.
5. **Monitor event-update** (D-0042 / runbook §60-86): make the monitor exit-on `escalation_opened_notice|escalation_bumped|escalation_stuck_resolved` + recognize the `[arhitect]` ntfy prefix. The current monitor catches new escalations via the open-SET change but MISSES a pure target-bump. Now that case-2b is live, safe to do.

## What ETAPA-5g deployed/decided (full detail: D-0045 + D-0046)
- **D-0045:** reconciled 5f (it exited on its own — no 2nd architect); independently re-verified the case-2b fix (PASS + mutation); consolidated-deployed case-2b (threshold 30 + scoped stuck-detector — flood ELIMINATED, 0 false pages) + **structural 120M→200M** + **critical 220M→250M** (both founder-approved); resolved [29][33] rework:VALIDATE + [34] rework:MERGE_GATE; agents stay 4; [23] auth-access re-confirm = founder decision-3 approved on green suite.
- **D-0046:** **structural 200M→250M** (founder-directed, proactive for status-notifications); deployed.
- **integration_validator tool-allowlist (419e554):** Part A above (deployed, ineffective-but-harmless).

## Founder threads / context
- **Redesign session:** ETAPA-5g built `claude_redesign.sh` — a SEPARATE parallel session (tmux `redesign`, RC `Redesign`) for discussion + the factory-REDESIGN task (interactive-tmux fleet, move agents off `claude -p` print-mode — see the `redesign-interactive-tmux-fleet` memory). It does NOT manage the live factory (scoped prompt forbids it). The founder coordinates redesign↔live integration manually. If he launches it, it's `./claude_redesign.sh`. The integration_validator overflow is arguably a print-mode-context-limit issue the redesign may address structurally — but the LIVE Part B fix is independent.
- **cap-5:** founder chose STAY 4.
- **codex + `-p` billing:** both working (auth-access dual-audit incl. codex passed; the 09:00 UTC watch window expired clean).

## Working-mode learnings (keep)
- **Redeploy ritual** (docs/runbooks/first-live-run.md, used twice today cleanly): disarm watchdog (`sudo -n systemctl disable --now sf-factory-watchdog.timer`) → stop monitor (exact-cmdline) → baseline `wc -l .factory/run-live.log` → `tmux send-keys -t factory C-c` → poll `kill -0 <orchpid>` until dead (~2s) → `fuser .factory/factory.db` = no holder → `tmux kill-session -t factory` → `tmux new-session -d -s factory -c /home/artur/projects/SF-F5 'export PATH="$HOME/.local/bin:$HOME/.nvm/versions/node/v24.16.0/bin:$PATH"; .venv/bin/sf-factory run 2>&1 | tee -a .factory/run-live.log'` → wait for `entering scheduler loop` past baseline (~10s, use 150s timeout) → re-arm watchdog → relaunch monitor → resolve escalations. **recalibrate→deploy→resolve order (D-0032): a config cap change must be LIVE before you resolve the budget escalation, else it re-fires.**
- **§10 lesson (fresh):** I trusted a subagent's token-attribution estimate (tools ≈ ~half the prompt) and shipped Part A — real first-use falsified it (tools were only ~16k). For Part B, the RETRY is the verification; don't declare success on an estimate.
- **Clean-window discipline:** deploy only with 0 EXPENSIVE factory agents in flight (frozen stages = stable window). Killing an in-flight builder/validator loses work. `pgrep -af 'output-format stream-json|codex exec' | grep -v -- '--remote-control'` = factory agent count (excludes your RC session + your subagents).
- **Monitor coverage gap (my mistake):** I once left only a targeted watcher (status-notifications) with no general session monitor → missed posting-engine's escalation (the founder caught it on the dashboard first). KEEP the general session monitor running at all times.
- **Self-match traps:** `pgrep -f`/`grep` for `sf-factory run`, `bin/python`, `sf-architect-monitor.sh`, or `venv/bin/sf-factory` ALL self-match your own shell (the strings are in your command/prompt). Filter by exact cmdline or exclude `$$`/`--remote-control`.
- **Founder:** Romanian, glossed (no bare IDs/escalation numbers), brutal honesty (§21), cost-conscious, drives by phone, front-loads decisions, watches the dashboard closely (caught an escalation before me). He chose 250M caps over my lower recs (prefers headroom / fewer interruptions).

## Your succession
Finish your work unit, write the handoff, launch ETAPA-5i, **VERIFY 5i's RC on the founder's phone BEFORE going silent** (predecessor RC is the fallback), hand the marker, go silent. Don't keep working after the successor takes the marker (sole-writer rule).
